# AXI-Stream UVM VIP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a complete UVM-based AXI4-Stream VIP with parameterized config, Master/Slave/Monitor agents, backpressure control, bandwidth monitoring, reset handling, phase jump, and a three-tier sequence library.

**Architecture:** Classic UVM agent architecture (axis_env → axis_agent → driver/sequencer/monitor) with env-level reset_handler, phase_controller, bandwidth_checker, protocol_checker, scoreboard, and coverage_collector. Bandwidth control embedded in drivers via bandwidth_controller component.

**Tech Stack:** SystemVerilog, UVM 1.2, SVA assertions. Cross-tool compatible (VCS/Xcelium/Questa).

**Spec:** `docs/superpowers/specs/2026-04-13-axis-vip-design.md`

**Note on testing:** This is a hardware verification project. "Testing" means: compile successfully → run simulation → check UVM report for PASS/FAIL. Each task ends with a compile check. Full simulation tests are in later tasks.

---

## File Structure

```
axis_vip/
├── src/
│   ├── axis_if.sv
│   ├── axis_types.sv
│   ├── axis_config.sv
│   ├── axis_transfer.sv
│   ├── axis_packet.sv
│   ├── axis_sequencer.sv
│   ├── axis_master_driver.sv
│   ├── axis_slave_driver.sv
│   ├── axis_monitor.sv
│   ├── axis_bandwidth_controller.sv
│   ├── axis_reset_listener.sv
│   ├── axis_agent.sv
│   ├── axis_reset_handler.sv
│   ├── axis_phase_controller.sv
│   ├── axis_protocol_checker.sv
│   ├── axis_protocol_checker_config.sv
│   ├── axis_scoreboard.sv
│   ├── axis_coverage_collector.sv
│   ├── axis_bandwidth_checker.sv
│   ├── axis_env.sv
│   └── axis_pkg.sv
├── sequences/
│   ├── axis_base_seq.sv
│   ├── axis_single_transfer_seq.sv
│   ├── axis_packet_seq.sv
│   ├── axis_idle_seq.sv
│   ├── axis_burst_seq.sv
│   ├── axis_backpressure_stress_seq.sv
│   ├── axis_interleave_seq.sv
│   ├── axis_error_inject_seq.sv
│   ├── axis_boundary_seq.sv
│   ├── axis_reset_during_transfer_seq.sv
│   ├── axis_base_vseq.sv
│   ├── axis_master_slave_sync_vseq.sv
│   ├── axis_bandwidth_sweep_vseq.sv
│   ├── axis_reset_recovery_vseq.sv
│   └── axis_full_stress_vseq.sv
├── tests/
│   ├── axis_base_test.sv
│   ├── axis_sanity_test.sv
│   ├── axis_backpressure_test.sv
│   ├── axis_bandwidth_test.sv
│   ├── axis_reset_test.sv
│   ├── axis_phase_jump_test.sv
│   └── axis_full_regression_test.sv
├── tb/
│   ├── tb_top.sv
│   └── axis_dummy_dut.sv
└── sim/
    ├── Makefile
    └── filelist.f
```

Key decisions:
- Split driver into `axis_master_driver.sv` and `axis_slave_driver.sv` (spec says one file, but master drives valid+payload while slave drives ready — different enough to warrant separate files)
- Added `axis_base_vseq.sv` for virtual sequence base class (holds sequencer handles)
- Package file `axis_pkg.sv` includes all files in dependency order

---

### Task 1: Types, Enums, and Interface

**Files:**
- Create: `axis_vip/src/axis_types.sv`
- Create: `axis_vip/src/axis_if.sv`

- [ ] **Step 1: Create directory structure**

```bash
mkdir -p axis_vip/{src,sequences,tests,tb,sim}
```

- [ ] **Step 2: Write axis_types.sv**

```systemverilog
// axis_vip/src/axis_types.sv
// All enums, typedefs, and structs for the AXI-Stream VIP

// Agent mode
typedef enum bit [1:0] {
    AXIS_MASTER       = 2'b00,
    AXIS_SLAVE        = 2'b01,
    AXIS_MONITOR_ONLY = 2'b10
} axis_agent_mode_e;

// Master valid generation mode
typedef enum bit [2:0] {
    VALID_ZERO_IDLE    = 3'b000,
    VALID_FIXED_IDLE   = 3'b001,
    VALID_RANDOM_IDLE  = 3'b010,
    VALID_WEIGHTED     = 3'b011,
    VALID_BURST_PAUSE  = 3'b100,
    VALID_PROFILE      = 3'b101
} axis_valid_gen_mode_e;

// Slave ready generation mode
typedef enum bit [2:0] {
    READY_ALWAYS         = 3'b000,
    READY_BEFORE_VALID   = 3'b001,
    READY_WITH_VALID     = 3'b010,
    READY_AFTER_VALID    = 3'b011,
    READY_WEIGHTED       = 3'b100,
    READY_TOGGLE         = 3'b101,
    READY_PROFILE        = 3'b110
} axis_ready_gen_mode_e;

// Reset polarity
typedef enum bit {
    AXIS_RESET_ACTIVE_LOW  = 1'b0,
    AXIS_RESET_ACTIVE_HIGH = 1'b1
} axis_reset_polarity_e;

// Reset sync mode
typedef enum bit {
    AXIS_RESET_SYNC  = 1'b0,
    AXIS_RESET_ASYNC = 1'b1
} axis_reset_sync_mode_e;

// Assertion severity
typedef enum bit [1:0] {
    AXIS_SEV_INFO    = 2'b00,
    AXIS_SEV_WARNING = 2'b01,
    AXIS_SEV_ERROR   = 2'b10,
    AXIS_SEV_FATAL   = 2'b11
} axis_severity_e;

// Valid profile entry
typedef struct {
    int unsigned            start_cycle;
    int unsigned            end_cycle;
    axis_valid_gen_mode_e   mode;
    int unsigned            idle_cycles;
    int unsigned            idle_min;
    int unsigned            idle_max;
    int unsigned            valid_weight;
    int unsigned            burst_len;
    int unsigned            pause_len;
} axis_valid_profile_entry_t;

// Ready profile entry
typedef struct {
    int unsigned            start_cycle;
    int unsigned            end_cycle;
    axis_ready_gen_mode_e   mode;
    int unsigned            ready_delay;
    int unsigned            ready_delay_min;
    int unsigned            ready_delay_max;
    int unsigned            ready_advance_cycles;
    int unsigned            ready_weight;
    int unsigned            ready_high;
    int unsigned            ready_low;
} axis_ready_profile_entry_t;

// Bandwidth profile entry
typedef struct {
    int unsigned    start_cycle;
    int unsigned    end_cycle;
    real            min_threshold;
    real            max_threshold;
} axis_bw_profile_entry_t;

// Sequence state (for virtual sequence state machine)
typedef enum bit [2:0] {
    AXIS_SEQ_STATE_NORMAL       = 3'b000,
    AXIS_SEQ_STATE_ERROR_INJECT = 3'b001,
    AXIS_SEQ_STATE_RECOVERY     = 3'b010,
    AXIS_SEQ_STATE_IDLE         = 3'b011,
    AXIS_SEQ_STATE_DONE         = 3'b100
} axis_seq_state_e;
```

- [ ] **Step 3: Write axis_if.sv**

```systemverilog
// axis_vip/src/axis_if.sv
// Parameterized AXI4-Stream interface

interface axis_if #(
    parameter int TDATA_WIDTH = 32,
    parameter int TID_WIDTH   = 4,
    parameter int TDEST_WIDTH = 4,
    parameter int TUSER_WIDTH = 1,
    parameter bit HAS_TSTRB   = 1,
    parameter bit HAS_TKEEP   = 1,
    parameter bit HAS_TLAST   = 1
)(
    input logic aclk,
    input logic aresetn
);

    // Signal declarations
    logic                       tvalid;
    logic                       tready;
    logic [TDATA_WIDTH-1:0]     tdata;
    logic [TDATA_WIDTH/8-1:0]   tstrb;
    logic [TDATA_WIDTH/8-1:0]   tkeep;
    logic                       tlast;
    logic [TID_WIDTH-1:0]       tid;
    logic [TDEST_WIDTH-1:0]     tdest;
    logic [TUSER_WIDTH-1:0]     tuser;

    // Master clocking block: drives payload, samples tready
    clocking master_cb @(posedge aclk);
        default input #1step output #0;
        output tvalid, tdata, tstrb, tkeep, tlast, tid, tdest, tuser;
        input  tready;
    endclocking

    // Slave clocking block: drives tready, samples payload
    clocking slave_cb @(posedge aclk);
        default input #1step output #0;
        output tready;
        input  tvalid, tdata, tstrb, tkeep, tlast, tid, tdest, tuser;
    endclocking

    // Monitor clocking block: samples all signals (non-driving)
    clocking monitor_cb @(posedge aclk);
        default input #1step;
        input tvalid, tready, tdata, tstrb, tkeep, tlast, tid, tdest, tuser;
    endclocking

    // Modports
    modport master_mp  (clocking master_cb,  input aclk, aresetn);
    modport slave_mp   (clocking slave_cb,   input aclk, aresetn);
    modport monitor_mp (clocking monitor_cb, input aclk, aresetn);

endinterface
```

- [ ] **Step 4: Commit**

```bash
git add axis_vip/src/axis_types.sv axis_vip/src/axis_if.sv
git commit -m "feat(axis-vip): add type definitions and parameterized interface"
```

---

### Task 2: Configuration Object

**Files:**
- Create: `axis_vip/src/axis_config.sv`
- Create: `axis_vip/src/axis_protocol_checker_config.sv`

- [ ] **Step 1: Write axis_config.sv**

```systemverilog
// axis_vip/src/axis_config.sv

class axis_config extends uvm_object;

    `uvm_object_utils(axis_config)

    // ---- Protocol parameters (static, set at elaboration) ----
    int unsigned TDATA_WIDTH = 32;
    int unsigned TID_WIDTH   = 4;
    int unsigned TDEST_WIDTH = 4;
    int unsigned TUSER_WIDTH = 1;
    bit          HAS_TSTRB   = 1;
    bit          HAS_TKEEP   = 1;
    bit          HAS_TLAST   = 1;

    // ---- Agent mode ----
    axis_agent_mode_e   agent_mode = AXIS_MASTER;
    uvm_active_passive_enum is_active = UVM_ACTIVE;

    // ---- Master valid generation strategy ----
    axis_valid_gen_mode_e   valid_gen_mode  = VALID_ZERO_IDLE;
    int unsigned            idle_cycles     = 0;
    int unsigned            idle_min        = 0;
    int unsigned            idle_max        = 5;
    int unsigned            valid_weight    = 80;
    int unsigned            burst_len       = 8;
    int unsigned            pause_len       = 4;
    axis_valid_profile_entry_t valid_profile[$];

    // ---- Slave ready generation strategy ----
    axis_ready_gen_mode_e   ready_gen_mode       = READY_ALWAYS;
    int unsigned            ready_delay          = 0;
    int unsigned            ready_delay_min      = 0;
    int unsigned            ready_delay_max      = 5;
    int unsigned            ready_advance_cycles = 1;
    int unsigned            ready_weight         = 60;
    int unsigned            ready_high           = 4;
    int unsigned            ready_low            = 2;
    axis_ready_profile_entry_t ready_profile[$];

    // ---- Bandwidth configuration ----
    bit          bw_check_enable   = 0;
    int unsigned bw_window_cycles  = 1000;
    real         bw_min_threshold  = 0.0;
    real         bw_max_threshold  = -1.0; // -1.0 means no upper limit
    axis_bw_profile_entry_t bw_profile[$];

    // ---- Reset configuration ----
    axis_reset_polarity_e   reset_polarity  = AXIS_RESET_ACTIVE_LOW;
    axis_reset_sync_mode_e  reset_sync_mode = AXIS_RESET_SYNC;
    bit                     hot_reset_enable = 0;

    // ---- Protocol checker config ----
    axis_protocol_checker_config checker_cfg;

    // ---- Runtime reconfiguration event ----
    uvm_event config_changed;

    function new(string name = "axis_config");
        super.new(name);
        config_changed = new("config_changed");
        checker_cfg = axis_protocol_checker_config::type_id::create("checker_cfg");
    endfunction

    // Derived helper: number of byte lanes
    function int unsigned get_byte_lanes();
        return TDATA_WIDTH / 8;
    endfunction

    // Trigger runtime reconfiguration notification
    function void notify_config_changed();
        config_changed.trigger();
    endfunction

endclass
```

- [ ] **Step 2: Write axis_protocol_checker_config.sv**

```systemverilog
// axis_vip/src/axis_protocol_checker_config.sv

class axis_protocol_checker_config extends uvm_object;

    `uvm_object_utils(axis_protocol_checker_config)

    // Global enable
    bit enable_all = 1;

    // Per-assertion enable
    bit enable_tvalid_stability    = 1;
    bit enable_tdata_stability     = 1;
    bit enable_tlast_integrity     = 1;
    bit enable_tid_consistency     = 1;
    bit enable_tdest_consistency   = 1;
    bit enable_tkeep_tstrb_relation = 1;
    bit enable_reset_signal_check  = 1;
    bit enable_x_z_check           = 1;
    bit enable_handshake_timeout   = 1;

    // Per-assertion severity
    axis_severity_e sev_tvalid_stability    = AXIS_SEV_ERROR;
    axis_severity_e sev_tdata_stability     = AXIS_SEV_ERROR;
    axis_severity_e sev_tlast_integrity     = AXIS_SEV_ERROR;
    axis_severity_e sev_tid_consistency     = AXIS_SEV_ERROR;
    axis_severity_e sev_tdest_consistency   = AXIS_SEV_ERROR;
    axis_severity_e sev_tkeep_tstrb_relation = AXIS_SEV_ERROR;
    axis_severity_e sev_reset_signal_check  = AXIS_SEV_ERROR;
    axis_severity_e sev_x_z_check           = AXIS_SEV_ERROR;
    axis_severity_e sev_handshake_timeout   = AXIS_SEV_WARNING;

    // Timeout value
    int unsigned handshake_timeout_cycles = 1000;

    function new(string name = "axis_protocol_checker_config");
        super.new(name);
    endfunction

    function void disable_all();
        enable_all = 0;
    endfunction

    function void set_enable_all();
        enable_all = 1;
        enable_tvalid_stability     = 1;
        enable_tdata_stability      = 1;
        enable_tlast_integrity      = 1;
        enable_tid_consistency      = 1;
        enable_tdest_consistency    = 1;
        enable_tkeep_tstrb_relation = 1;
        enable_reset_signal_check   = 1;
        enable_x_z_check            = 1;
        enable_handshake_timeout    = 1;
    endfunction

endclass
```

- [ ] **Step 3: Commit**

```bash
git add axis_vip/src/axis_config.sv axis_vip/src/axis_protocol_checker_config.sv
git commit -m "feat(axis-vip): add configuration objects"
```

---

### Task 3: Transaction Objects

**Files:**
- Create: `axis_vip/src/axis_transfer.sv`
- Create: `axis_vip/src/axis_packet.sv`

- [ ] **Step 1: Write axis_transfer.sv (beat-level transaction)**

```systemverilog
// axis_vip/src/axis_transfer.sv

class axis_transfer extends uvm_sequence_item;

    // Payload fields — use max width, actual width controlled by config
    rand bit [511:0]  tdata;
    rand bit [63:0]   tstrb;
    rand bit [63:0]   tkeep;
    rand bit          tlast;
    rand bit [15:0]   tid;
    rand bit [15:0]   tdest;
    rand bit [127:0]  tuser;

    // Timing
    rand int unsigned delay; // idle cycles before this beat

    // Config reference (set by sequence, not randomized)
    axis_config cfg;

    `uvm_object_utils_begin(axis_transfer)
        `uvm_field_int(tdata,  UVM_ALL_ON)
        `uvm_field_int(tstrb,  UVM_ALL_ON)
        `uvm_field_int(tkeep,  UVM_ALL_ON)
        `uvm_field_int(tlast,  UVM_ALL_ON)
        `uvm_field_int(tid,    UVM_ALL_ON)
        `uvm_field_int(tdest,  UVM_ALL_ON)
        `uvm_field_int(tuser,  UVM_ALL_ON)
        `uvm_field_int(delay,  UVM_ALL_ON)
    `uvm_object_utils_end

    function new(string name = "axis_transfer");
        super.new(name);
    endfunction

    // Constrain fields to configured widths
    constraint c_data_width {
        (cfg != null) -> tdata inside {[0 : (1 << cfg.TDATA_WIDTH) - 1]};
    }

    constraint c_strb_width {
        (cfg != null) -> tstrb inside {[0 : (1 << cfg.get_byte_lanes()) - 1]};
    }

    constraint c_keep_width {
        (cfg != null) -> tkeep inside {[0 : (1 << cfg.get_byte_lanes()) - 1]};
    }

    constraint c_tid_width {
        (cfg != null) -> tid inside {[0 : (1 << cfg.TID_WIDTH) - 1]};
    }

    constraint c_tdest_width {
        (cfg != null) -> tdest inside {[0 : (1 << cfg.TDEST_WIDTH) - 1]};
    }

    constraint c_tuser_width {
        (cfg != null) -> tuser inside {[0 : (1 << cfg.TUSER_WIDTH) - 1]};
    }

    // TSTRB can only be 1 where TKEEP is 1
    constraint c_tkeep_tstrb {
        (cfg != null && cfg.HAS_TSTRB && cfg.HAS_TKEEP) -> (tstrb & ~tkeep) == 0;
    }

    constraint c_delay {
        delay inside {[0:20]};
    }

    // Default: all keep bytes valid
    constraint c_keep_default {
        soft tkeep == (1 << ((cfg != null) ? cfg.get_byte_lanes() : 4)) - 1;
    }

    constraint c_strb_default {
        soft tstrb == tkeep;
    }

endclass
```

- [ ] **Step 2: Write axis_packet.sv (packet-level transaction)**

```systemverilog
// axis_vip/src/axis_packet.sv

class axis_packet extends uvm_sequence_item;

    axis_transfer beats[$];
    bit [15:0]    tid;
    bit [15:0]    tdest;
    int unsigned  packet_length;
    time          timestamp;

    `uvm_object_utils_begin(axis_packet)
        `uvm_field_int(tid,           UVM_ALL_ON)
        `uvm_field_int(tdest,         UVM_ALL_ON)
        `uvm_field_int(packet_length, UVM_ALL_ON)
        `uvm_field_int(timestamp,     UVM_ALL_ON | UVM_TIME)
    `uvm_object_utils_end

    function new(string name = "axis_packet");
        super.new(name);
    endfunction

    // Add a beat to the packet
    function void add_beat(axis_transfer beat);
        beats.push_back(beat);
        if (beats.size() == 1) begin
            tid       = beat.tid;
            tdest     = beat.tdest;
            timestamp = $time;
        end
        packet_length = beats.size();
    endfunction

    // Check if packet is complete (last beat has tlast=1)
    function bit is_complete();
        if (beats.size() == 0) return 0;
        return beats[beats.size()-1].tlast;
    endfunction

    // Get all tdata concatenated for comparison
    function void get_payload(ref bit [7:0] payload[$]);
        payload.delete();
        foreach (beats[i]) begin
            for (int b = 0; b < 64; b++) begin // max byte lanes
                if (beats[i].tkeep[b])
                    payload.push_back(beats[i].tdata[b*8 +: 8]);
            end
        end
    endfunction

    // Compare payload with another packet
    function bit compare_payload(axis_packet other);
        bit [7:0] this_payload[$];
        bit [7:0] other_payload[$];
        this.get_payload(this_payload);
        other.get_payload(other_payload);
        if (this_payload.size() != other_payload.size()) return 0;
        foreach (this_payload[i])
            if (this_payload[i] !== other_payload[i]) return 0;
        return 1;
    endfunction

endclass
```

- [ ] **Step 3: Commit**

```bash
git add axis_vip/src/axis_transfer.sv axis_vip/src/axis_packet.sv
git commit -m "feat(axis-vip): add beat-level and packet-level transaction objects"
```

---

### Task 4: Sequencer

**Files:**
- Create: `axis_vip/src/axis_sequencer.sv`

- [ ] **Step 1: Write axis_sequencer.sv**

```systemverilog
// axis_vip/src/axis_sequencer.sv

class axis_sequencer extends uvm_sequencer #(axis_transfer);

    `uvm_component_utils(axis_sequencer)

    axis_config cfg;

    // Reset control: when set, sequences should stop
    bit reset_active = 0;

    // For hot reset: stores the last sequence type name for restart
    string last_seq_type_name;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
    endfunction

    // Called by reset_listener to flush pending transactions
    function void flush_pending();
        // Stop all sequences and clear the FIFO
        stop_sequences();
        `uvm_info(get_type_name(), "Flushed pending transactions due to reset", UVM_MEDIUM)
    endfunction

    // Signal reset state to running sequences
    function void set_reset_active(bit active);
        reset_active = active;
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_sequencer.sv
git commit -m "feat(axis-vip): add sequencer with reset control"
```

---

### Task 5: Bandwidth Controller

**Files:**
- Create: `axis_vip/src/axis_bandwidth_controller.sv`

- [ ] **Step 1: Write axis_bandwidth_controller.sv**

```systemverilog
// axis_vip/src/axis_bandwidth_controller.sv
// Per-agent component that decides idle/ready insertion per clock cycle

class axis_bandwidth_controller extends uvm_component;

    `uvm_component_utils(axis_bandwidth_controller)

    axis_config cfg;

    // Internal state
    protected int unsigned cycle_count;
    protected int unsigned burst_beat_count;
    protected bit          in_pause;
    protected int unsigned pause_count;
    protected int unsigned toggle_count;
    protected bit          toggle_state; // 1=high, 0=low

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
        reset_state();
    endfunction

    function void reset_state();
        cycle_count      = 0;
        burst_beat_count = 0;
        in_pause         = 0;
        pause_count      = 0;
        toggle_count     = 0;
        toggle_state     = 1;
    endfunction

    // ---- Master side: compute idle cycles to insert after a handshake ----
    function int unsigned get_valid_idle_cycles();
        case (cfg.valid_gen_mode)
            VALID_ZERO_IDLE:   return 0;
            VALID_FIXED_IDLE:  return cfg.idle_cycles;
            VALID_RANDOM_IDLE: return $urandom_range(cfg.idle_min, cfg.idle_max);
            VALID_WEIGHTED:    return 0; // handled by should_assert_valid()
            VALID_BURST_PAUSE: return 0; // handled by should_assert_valid()
            VALID_PROFILE:     return get_profile_valid_idle_cycles();
            default:           return 0;
        endcase
    endfunction

    // For WEIGHTED and BURST_PAUSE modes: should valid be driven this cycle?
    function bit should_assert_valid();
        cycle_count++;
        case (cfg.valid_gen_mode)
            VALID_WEIGHTED: begin
                return ($urandom_range(0, 99) < cfg.valid_weight);
            end
            VALID_BURST_PAUSE: begin
                if (!in_pause) begin
                    burst_beat_count++;
                    if (burst_beat_count >= cfg.burst_len) begin
                        in_pause = 1;
                        burst_beat_count = 0;
                        pause_count = 0;
                    end
                    return 1;
                end else begin
                    pause_count++;
                    if (pause_count >= cfg.pause_len) begin
                        in_pause = 0;
                        pause_count = 0;
                    end
                    return 0;
                end
            end
            VALID_PROFILE: return get_profile_should_assert_valid();
            default: return 1;
        endcase
    endfunction

    // ---- Slave side: should TREADY be asserted this cycle? ----
    function bit should_assert_ready(bit tvalid_seen);
        cycle_count++;
        case (cfg.ready_gen_mode)
            READY_ALWAYS:       return 1;
            READY_BEFORE_VALID: return 1; // always high, proactive
            READY_WITH_VALID:   return tvalid_seen;
            READY_AFTER_VALID:  return 0; // delay handled in driver
            READY_WEIGHTED:     return ($urandom_range(0, 99) < cfg.ready_weight);
            READY_TOGGLE: begin
                toggle_count++;
                if (toggle_state && toggle_count >= cfg.ready_high) begin
                    toggle_state = 0;
                    toggle_count = 0;
                end else if (!toggle_state && toggle_count >= cfg.ready_low) begin
                    toggle_state = 1;
                    toggle_count = 0;
                end
                return toggle_state;
            end
            READY_PROFILE: return get_profile_should_assert_ready(tvalid_seen);
            default: return 1;
        endcase
    endfunction

    // Get ready delay for READY_AFTER_VALID mode
    function int unsigned get_ready_delay();
        if (cfg.ready_delay_min == cfg.ready_delay_max)
            return cfg.ready_delay;
        return $urandom_range(cfg.ready_delay_min, cfg.ready_delay_max);
    endfunction

    // ---- Profile helpers ----

    protected function axis_valid_profile_entry_t get_current_valid_profile();
        foreach (cfg.valid_profile[i]) begin
            if (cycle_count >= cfg.valid_profile[i].start_cycle &&
                cycle_count <= cfg.valid_profile[i].end_cycle)
                return cfg.valid_profile[i];
        end
        // Default: zero idle
        return '{start_cycle: 0, end_cycle: '1, mode: VALID_ZERO_IDLE,
                 idle_cycles: 0, idle_min: 0, idle_max: 0,
                 valid_weight: 100, burst_len: 8, pause_len: 4};
    endfunction

    protected function int unsigned get_profile_valid_idle_cycles();
        axis_valid_profile_entry_t entry = get_current_valid_profile();
        case (entry.mode)
            VALID_ZERO_IDLE:   return 0;
            VALID_FIXED_IDLE:  return entry.idle_cycles;
            VALID_RANDOM_IDLE: return $urandom_range(entry.idle_min, entry.idle_max);
            default:           return 0;
        endcase
    endfunction

    protected function bit get_profile_should_assert_valid();
        axis_valid_profile_entry_t entry = get_current_valid_profile();
        case (entry.mode)
            VALID_WEIGHTED: return ($urandom_range(0, 99) < entry.valid_weight);
            default:        return 1;
        endcase
    endfunction

    protected function axis_ready_profile_entry_t get_current_ready_profile();
        foreach (cfg.ready_profile[i]) begin
            if (cycle_count >= cfg.ready_profile[i].start_cycle &&
                cycle_count <= cfg.ready_profile[i].end_cycle)
                return cfg.ready_profile[i];
        end
        // Default: always ready
        return '{start_cycle: 0, end_cycle: '1, mode: READY_ALWAYS,
                 ready_delay: 0, ready_delay_min: 0, ready_delay_max: 0,
                 ready_advance_cycles: 1, ready_weight: 100,
                 ready_high: 4, ready_low: 2};
    endfunction

    protected function bit get_profile_should_assert_ready(bit tvalid_seen);
        axis_ready_profile_entry_t entry = get_current_ready_profile();
        case (entry.mode)
            READY_ALWAYS:       return 1;
            READY_BEFORE_VALID: return 1;
            READY_WITH_VALID:   return tvalid_seen;
            READY_WEIGHTED:     return ($urandom_range(0, 99) < entry.ready_weight);
            READY_TOGGLE: begin
                // Simplified: use top-level toggle state
                return toggle_state;
            end
            default: return 1;
        endcase
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_bandwidth_controller.sv
git commit -m "feat(axis-vip): add bandwidth controller for valid/ready generation"
```

---

### Task 6: Reset Listener

**Files:**
- Create: `axis_vip/src/axis_reset_listener.sv`

- [ ] **Step 1: Write axis_reset_listener.sv**

```systemverilog
// axis_vip/src/axis_reset_listener.sv
// Per-agent component: subscribes to env-level reset events, coordinates local response

class axis_reset_listener extends uvm_component;

    `uvm_component_utils(axis_reset_listener)

    axis_config cfg;

    // Events from env-level reset_handler (set during connect_phase)
    uvm_event reset_asserted_evt;
    uvm_event reset_active_evt;
    uvm_event reset_deasserted_evt;

    // Local references (set by agent during connect_phase)
    axis_sequencer           sqr;
    axis_bandwidth_controller bw_ctrl;

    // State
    bit is_in_reset = 0;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
    endfunction

    task run_phase(uvm_phase phase);
        forever begin
            // Wait for reset assertion
            reset_asserted_evt.wait_trigger();
            is_in_reset = 1;
            `uvm_info(get_type_name(), "Reset asserted - notifying agent components", UVM_MEDIUM)

            // Notify sequencer
            if (sqr != null) begin
                sqr.set_reset_active(1);
                sqr.flush_pending();
            end

            // Reset bandwidth controller state
            if (bw_ctrl != null)
                bw_ctrl.reset_state();

            // Wait for reset deassert
            reset_deasserted_evt.wait_trigger();
            is_in_reset = 0;
            `uvm_info(get_type_name(), "Reset deasserted - agent ready to resume", UVM_MEDIUM)

            // Notify sequencer
            if (sqr != null)
                sqr.set_reset_active(0);

            // Hot reset: auto-restart last sequence
            if (cfg.hot_reset_enable && sqr != null && sqr.last_seq_type_name != "") begin
                `uvm_info(get_type_name(),
                    $sformatf("Hot reset: restarting sequence '%s'", sqr.last_seq_type_name),
                    UVM_MEDIUM)
                // Sequence restart is handled by the test/vseq layer
                // We just signal that the sequencer is ready
            end
        end
    endtask

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_reset_listener.sv
git commit -m "feat(axis-vip): add per-agent reset listener"
```

---

### Task 7: Master Driver

**Files:**
- Create: `axis_vip/src/axis_master_driver.sv`

- [ ] **Step 1: Write axis_master_driver.sv**

```systemverilog
// axis_vip/src/axis_master_driver.sv

class axis_master_driver extends uvm_driver #(axis_transfer);

    `uvm_component_utils(axis_master_driver)

    virtual axis_if vif;
    axis_config cfg;
    axis_bandwidth_controller bw_ctrl;

    // Reset state
    bit in_reset = 0;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
        if (!uvm_config_db#(virtual axis_if)::get(this, "", "vif", vif))
            `uvm_fatal("NOVIF", "Virtual interface not found in config_db")
    endfunction

    task run_phase(uvm_phase phase);
        // Initialize outputs
        drive_reset_values();

        forever begin
            if (in_reset) begin
                drive_reset_values();
                @(posedge vif.aclk);
                continue;
            end

            seq_item_port.get_next_item(req);

            if (in_reset) begin
                seq_item_port.item_done();
                continue;
            end

            drive_transfer(req);
            seq_item_port.item_done();
        end
    endtask

    // Drive a single beat transfer
    protected task drive_transfer(axis_transfer tr);
        int unsigned idle_cycles;

        // Compute idle cycles based on bandwidth mode
        case (cfg.valid_gen_mode)
            VALID_WEIGHTED, VALID_BURST_PAUSE, VALID_PROFILE: begin
                // For these modes, use should_assert_valid() to gate each cycle
                while (!bw_ctrl.should_assert_valid()) begin
                    if (in_reset) return;
                    vif.master_cb.tvalid <= 1'b0;
                    @(vif.master_cb);
                end
            end
            default: begin
                // Use delay from transaction or bandwidth controller
                idle_cycles = (tr.delay > 0) ? tr.delay : bw_ctrl.get_valid_idle_cycles();
                repeat (idle_cycles) begin
                    if (in_reset) return;
                    vif.master_cb.tvalid <= 1'b0;
                    @(vif.master_cb);
                end
            end
        endcase

        // Drive valid + payload
        vif.master_cb.tvalid <= 1'b1;
        vif.master_cb.tdata  <= tr.tdata[cfg.TDATA_WIDTH-1:0];
        if (cfg.HAS_TSTRB) vif.master_cb.tstrb <= tr.tstrb[cfg.get_byte_lanes()-1:0];
        if (cfg.HAS_TKEEP) vif.master_cb.tkeep <= tr.tkeep[cfg.get_byte_lanes()-1:0];
        if (cfg.HAS_TLAST) vif.master_cb.tlast <= tr.tlast;
        vif.master_cb.tid   <= tr.tid[cfg.TID_WIDTH-1:0];
        vif.master_cb.tdest <= tr.tdest[cfg.TDEST_WIDTH-1:0];
        vif.master_cb.tuser <= tr.tuser[cfg.TUSER_WIDTH-1:0];

        // Wait for handshake (valid & ready)
        @(vif.master_cb);
        while (!vif.master_cb.tready) begin
            if (in_reset) return;
            @(vif.master_cb);
        end

        // Handshake complete — deassert valid
        vif.master_cb.tvalid <= 1'b0;
    endtask

    function void drive_reset_values();
        vif.master_cb.tvalid <= 1'b0;
        vif.master_cb.tdata  <= '0;
        vif.master_cb.tstrb  <= '0;
        vif.master_cb.tkeep  <= '0;
        vif.master_cb.tlast  <= 1'b0;
        vif.master_cb.tid    <= '0;
        vif.master_cb.tdest  <= '0;
        vif.master_cb.tuser  <= '0;
    endfunction

    function void set_in_reset(bit rst);
        in_reset = rst;
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_master_driver.sv
git commit -m "feat(axis-vip): add master driver with bandwidth-controlled valid generation"
```

---

### Task 8: Slave Driver

**Files:**
- Create: `axis_vip/src/axis_slave_driver.sv`

- [ ] **Step 1: Write axis_slave_driver.sv**

```systemverilog
// axis_vip/src/axis_slave_driver.sv

class axis_slave_driver extends uvm_driver #(axis_transfer);

    `uvm_component_utils(axis_slave_driver)

    virtual axis_if vif;
    axis_config cfg;
    axis_bandwidth_controller bw_ctrl;

    // Reset state
    bit in_reset = 0;

    // For READY_AFTER_VALID: track valid seen
    protected bit valid_seen = 0;
    protected int unsigned delay_counter = 0;
    protected int unsigned target_delay = 0;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
        if (!uvm_config_db#(virtual axis_if)::get(this, "", "vif", vif))
            `uvm_fatal("NOVIF", "Virtual interface not found in config_db")
    endfunction

    task run_phase(uvm_phase phase);
        drive_reset_values();

        forever begin
            if (in_reset) begin
                drive_reset_values();
                valid_seen = 0;
                delay_counter = 0;
                @(posedge vif.aclk);
                continue;
            end

            drive_ready();
            @(vif.slave_cb);
        end
    endtask

    protected task drive_ready();
        bit tvalid_current;
        tvalid_current = vif.slave_cb.tvalid;

        case (cfg.ready_gen_mode)
            READY_ALWAYS: begin
                vif.slave_cb.tready <= 1'b1;
            end

            READY_BEFORE_VALID: begin
                // Proactively assert ready
                vif.slave_cb.tready <= 1'b1;
            end

            READY_WITH_VALID: begin
                vif.slave_cb.tready <= tvalid_current;
            end

            READY_AFTER_VALID: begin
                if (tvalid_current && !valid_seen) begin
                    valid_seen = 1;
                    target_delay = bw_ctrl.get_ready_delay();
                    delay_counter = 0;
                end

                if (valid_seen) begin
                    if (delay_counter >= target_delay) begin
                        vif.slave_cb.tready <= 1'b1;
                        // After handshake, reset state
                        if (tvalid_current) begin
                            valid_seen = 0;
                            delay_counter = 0;
                        end
                    end else begin
                        vif.slave_cb.tready <= 1'b0;
                        delay_counter++;
                    end
                end else begin
                    vif.slave_cb.tready <= 1'b0;
                end
            end

            READY_WEIGHTED: begin
                vif.slave_cb.tready <= bw_ctrl.should_assert_ready(tvalid_current);
            end

            READY_TOGGLE: begin
                vif.slave_cb.tready <= bw_ctrl.should_assert_ready(tvalid_current);
            end

            READY_PROFILE: begin
                vif.slave_cb.tready <= bw_ctrl.should_assert_ready(tvalid_current);
            end

            default: begin
                vif.slave_cb.tready <= 1'b1;
            end
        endcase
    endtask

    function void drive_reset_values();
        vif.slave_cb.tready <= 1'b0;
    endfunction

    function void set_in_reset(bit rst);
        in_reset = rst;
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_slave_driver.sv
git commit -m "feat(axis-vip): add slave driver with ready generation strategies"
```

---

### Task 9: Monitor

**Files:**
- Create: `axis_vip/src/axis_monitor.sv`

- [ ] **Step 1: Write axis_monitor.sv**

```systemverilog
// axis_vip/src/axis_monitor.sv

class axis_monitor extends uvm_monitor;

    `uvm_component_utils(axis_monitor)

    virtual axis_if vif;
    axis_config cfg;

    // Analysis ports
    uvm_analysis_port #(axis_transfer) beat_ap;   // beat-level
    uvm_analysis_port #(axis_packet)   packet_ap; // packet-level

    // Packet assembly: indexed by TID for interleaved streams
    protected axis_packet in_progress_packets[bit[15:0]];

    // Reset state
    bit in_reset = 0;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        beat_ap   = new("beat_ap",   this);
        packet_ap = new("packet_ap", this);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
        if (!uvm_config_db#(virtual axis_if)::get(this, "", "vif", vif))
            `uvm_fatal("NOVIF", "Virtual interface not found in config_db")
    endfunction

    task run_phase(uvm_phase phase);
        forever begin
            if (in_reset) begin
                flush_in_progress();
                @(posedge vif.aclk);
                continue;
            end

            @(vif.monitor_cb);
            if (vif.monitor_cb.tvalid && vif.monitor_cb.tready) begin
                sample_beat();
            end
        end
    endtask

    protected function void sample_beat();
        axis_transfer tr = axis_transfer::type_id::create("tr");
        tr.cfg   = cfg;
        tr.tdata = vif.monitor_cb.tdata;
        tr.tstrb = cfg.HAS_TSTRB ? vif.monitor_cb.tstrb : '1;
        tr.tkeep = cfg.HAS_TKEEP ? vif.monitor_cb.tkeep : '1;
        tr.tlast = cfg.HAS_TLAST ? vif.monitor_cb.tlast : 1'b0;
        tr.tid   = vif.monitor_cb.tid;
        tr.tdest = vif.monitor_cb.tdest;
        tr.tuser = vif.monitor_cb.tuser;

        // Publish beat
        beat_ap.write(tr);

        // Assemble into packet (keyed by TID)
        if (!in_progress_packets.exists(tr.tid)) begin
            in_progress_packets[tr.tid] = axis_packet::type_id::create(
                $sformatf("pkt_tid%0d", tr.tid));
        end

        in_progress_packets[tr.tid].add_beat(tr);

        // If TLAST, publish completed packet
        if (tr.tlast || !cfg.HAS_TLAST) begin
            packet_ap.write(in_progress_packets[tr.tid]);
            in_progress_packets.delete(tr.tid);
        end
    endfunction

    protected function void flush_in_progress();
        in_progress_packets.delete();
    endfunction

    function void set_in_reset(bit rst);
        in_reset = rst;
        if (rst) flush_in_progress();
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_monitor.sv
git commit -m "feat(axis-vip): add monitor with beat and packet analysis ports"
```

---

### Task 10: Agent

**Files:**
- Create: `axis_vip/src/axis_agent.sv`

- [ ] **Step 1: Write axis_agent.sv**

```systemverilog
// axis_vip/src/axis_agent.sv

class axis_agent extends uvm_agent;

    `uvm_component_utils(axis_agent)

    axis_config              cfg;
    axis_sequencer           sqr;
    axis_master_driver       m_drv;
    axis_slave_driver        s_drv;
    axis_monitor             mon;
    axis_bandwidth_controller bw_ctrl;
    axis_reset_listener      rst_listener;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);

        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")

        // Always build monitor
        mon = axis_monitor::type_id::create("mon", this);

        // Always build reset listener
        rst_listener = axis_reset_listener::type_id::create("rst_listener", this);

        // Build active components if not MONITOR_ONLY and is_active
        if (cfg.agent_mode != AXIS_MONITOR_ONLY && cfg.is_active == UVM_ACTIVE) begin
            sqr     = axis_sequencer::type_id::create("sqr", this);
            bw_ctrl = axis_bandwidth_controller::type_id::create("bw_ctrl", this);

            if (cfg.agent_mode == AXIS_MASTER)
                m_drv = axis_master_driver::type_id::create("m_drv", this);
            else if (cfg.agent_mode == AXIS_SLAVE)
                s_drv = axis_slave_driver::type_id::create("s_drv", this);
        end
    endfunction

    function void connect_phase(uvm_phase phase);
        super.connect_phase(phase);

        if (cfg.agent_mode != AXIS_MONITOR_ONLY && cfg.is_active == UVM_ACTIVE) begin
            if (cfg.agent_mode == AXIS_MASTER && m_drv != null) begin
                m_drv.seq_item_port.connect(sqr.seq_item_export);
                m_drv.bw_ctrl = bw_ctrl;
            end else if (cfg.agent_mode == AXIS_SLAVE && s_drv != null) begin
                s_drv.seq_item_port.connect(sqr.seq_item_export);
                s_drv.bw_ctrl = bw_ctrl;
            end

            // Connect reset listener to local components
            rst_listener.sqr     = sqr;
            rst_listener.bw_ctrl = bw_ctrl;
        end
    endfunction

    // Called by env-level reset_handler to propagate reset
    function void set_in_reset(bit rst);
        mon.set_in_reset(rst);
        if (m_drv != null) m_drv.set_in_reset(rst);
        if (s_drv != null) s_drv.set_in_reset(rst);
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_agent.sv
git commit -m "feat(axis-vip): add agent with conditional master/slave/monitor instantiation"
```

---

### Task 11: Reset Handler (env-level)

**Files:**
- Create: `axis_vip/src/axis_reset_handler.sv`

- [ ] **Step 1: Write axis_reset_handler.sv**

```systemverilog
// axis_vip/src/axis_reset_handler.sv
// Env-level reset coordinator

class axis_reset_handler extends uvm_component;

    `uvm_component_utils(axis_reset_handler)

    virtual axis_if vif;
    axis_config cfg;

    // Reset events — shared with all reset_listeners
    uvm_event reset_asserted_evt;
    uvm_event reset_active_evt;
    uvm_event reset_deasserted_evt;

    // Agents to notify (set by env during connect_phase)
    axis_agent agents[$];

    // State
    bit is_in_reset = 0;

    function new(string name, uvm_component parent);
        super.new(name, parent);
        reset_asserted_evt   = new("reset_asserted_evt");
        reset_active_evt     = new("reset_active_evt");
        reset_deasserted_evt = new("reset_deasserted_evt");
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
        if (!uvm_config_db#(virtual axis_if)::get(this, "", "vif", vif))
            `uvm_fatal("NOVIF", "Virtual interface not found in config_db")
    endfunction

    task run_phase(uvm_phase phase);
        // Wait for initial reset to complete
        wait_for_reset_done();

        forever begin
            // Detect reset assertion
            if (cfg.reset_sync_mode == AXIS_RESET_SYNC)
                wait_for_sync_reset_assert();
            else
                wait_for_async_reset_assert();

            handle_reset_assert();

            // Wait for reset deassertion
            if (cfg.reset_sync_mode == AXIS_RESET_SYNC)
                wait_for_sync_reset_deassert();
            else
                wait_for_async_reset_deassert();

            handle_reset_deassert();
        end
    endtask

    // ---- Sync reset detection ----
    protected task wait_for_sync_reset_assert();
        if (cfg.reset_polarity == AXIS_RESET_ACTIVE_LOW) begin
            @(posedge vif.aclk);
            while (vif.aresetn !== 1'b0) @(posedge vif.aclk);
        end else begin
            @(posedge vif.aclk);
            while (vif.aresetn !== 1'b1) @(posedge vif.aclk);
        end
    endtask

    protected task wait_for_sync_reset_deassert();
        if (cfg.reset_polarity == AXIS_RESET_ACTIVE_LOW) begin
            @(posedge vif.aclk);
            while (vif.aresetn !== 1'b1) @(posedge vif.aclk);
        end else begin
            @(posedge vif.aclk);
            while (vif.aresetn !== 1'b0) @(posedge vif.aclk);
        end
    endtask

    // ---- Async reset detection ----
    protected task wait_for_async_reset_assert();
        if (cfg.reset_polarity == AXIS_RESET_ACTIVE_LOW)
            @(negedge vif.aresetn);
        else
            @(posedge vif.aresetn);
    endtask

    protected task wait_for_async_reset_deassert();
        if (cfg.reset_polarity == AXIS_RESET_ACTIVE_LOW)
            @(posedge vif.aresetn);
        else
            @(negedge vif.aresetn);
    endtask

    // ---- Initial reset ----
    protected task wait_for_reset_done();
        if (cfg.reset_polarity == AXIS_RESET_ACTIVE_LOW) begin
            if (vif.aresetn === 1'b0) begin
                `uvm_info(get_type_name(), "Waiting for initial reset to complete", UVM_MEDIUM)
                @(posedge vif.aresetn);
            end
        end else begin
            if (vif.aresetn === 1'b1) begin
                `uvm_info(get_type_name(), "Waiting for initial reset to complete", UVM_MEDIUM)
                @(negedge vif.aresetn);
            end
        end
        `uvm_info(get_type_name(), "Initial reset complete", UVM_MEDIUM)
    endtask

    // ---- Handlers ----
    protected function void handle_reset_assert();
        is_in_reset = 1;
        `uvm_info(get_type_name(), "Reset asserted", UVM_MEDIUM)

        // Notify all agents
        foreach (agents[i])
            agents[i].set_in_reset(1);

        // Trigger events for reset_listeners
        reset_asserted_evt.trigger();
        reset_active_evt.trigger();
    endfunction

    protected function void handle_reset_deassert();
        is_in_reset = 0;
        `uvm_info(get_type_name(), "Reset deasserted", UVM_MEDIUM)

        // Notify all agents
        foreach (agents[i])
            agents[i].set_in_reset(0);

        // Trigger event
        reset_deasserted_evt.trigger();
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_reset_handler.sv
git commit -m "feat(axis-vip): add env-level reset handler with sync/async support"
```

---

### Task 12: Phase Controller

**Files:**
- Create: `axis_vip/src/axis_phase_controller.sv`

- [ ] **Step 1: Write axis_phase_controller.sv**

```systemverilog
// axis_vip/src/axis_phase_controller.sv

class axis_phase_controller extends uvm_component;

    `uvm_component_utils(axis_phase_controller)

    axis_config cfg;
    axis_reset_handler rst_handler;

    // Drain timeout in clock cycles
    int unsigned drain_timeout = 1000;

    // Agents to drain (set by env)
    axis_agent agents[$];

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
    endfunction

    // Request a phase jump with in-flight drain
    task request_phase_jump(uvm_phase current_phase, uvm_phase target_phase);
        // Block if reset is active
        if (rst_handler != null && rst_handler.is_in_reset) begin
            `uvm_warning(get_type_name(), "Phase jump blocked: reset is active")
            return;
        end

        `uvm_info(get_type_name(),
            $sformatf("Phase jump requested: %s -> %s", current_phase.get_name(), target_phase.get_name()),
            UVM_LOW)

        // Step 1: Signal all sequencers to stop
        foreach (agents[i]) begin
            if (agents[i].sqr != null)
                agents[i].sqr.set_reset_active(1); // reuse reset flag to stop new items
        end

        // Step 2: Wait for in-flight transactions to drain
        drain_in_flight();

        // Step 3: Execute phase jump
        current_phase.jump(target_phase);

        // Step 4: Resume sequencers
        foreach (agents[i]) begin
            if (agents[i].sqr != null)
                agents[i].sqr.set_reset_active(0);
        end

        `uvm_info(get_type_name(), "Phase jump complete", UVM_LOW)
    endtask

    // Wait for all in-flight transactions to complete or timeout
    protected task drain_in_flight();
        int unsigned timeout_count = 0;
        bit all_drained = 0;

        `uvm_info(get_type_name(), "Draining in-flight transactions...", UVM_MEDIUM)

        while (!all_drained && timeout_count < drain_timeout) begin
            all_drained = 1;
            foreach (agents[i]) begin
                if (agents[i].sqr != null && agents[i].sqr.has_do_available()) begin
                    all_drained = 0;
                    break;
                end
            end
            if (!all_drained) begin
                #1;
                timeout_count++;
            end
        end

        if (!all_drained)
            `uvm_warning(get_type_name(),
                $sformatf("Drain timeout after %0d cycles, forcing phase jump", drain_timeout))
        else
            `uvm_info(get_type_name(), "All in-flight transactions drained", UVM_MEDIUM)
    endtask

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_phase_controller.sv
git commit -m "feat(axis-vip): add phase controller with drain mechanism"
```

---

### Task 13: Protocol Checker

**Files:**
- Create: `axis_vip/src/axis_protocol_checker.sv`

- [ ] **Step 1: Write axis_protocol_checker.sv**

```systemverilog
// axis_vip/src/axis_protocol_checker.sv

class axis_protocol_checker extends uvm_component;

    `uvm_component_utils(axis_protocol_checker)

    virtual axis_if vif;
    axis_config cfg;
    axis_protocol_checker_config checker_cfg;

    // Internal state for stability checks
    protected logic prev_tvalid;
    protected logic [511:0] prev_tdata;
    protected logic [63:0]  prev_tstrb;
    protected logic [63:0]  prev_tkeep;
    protected logic         prev_tlast;
    protected logic [15:0]  prev_tid;
    protected logic [15:0]  prev_tdest;
    protected logic [127:0] prev_tuser;

    // Packet tracking for TID/TDEST consistency
    protected bit [15:0] pkt_tid[bit[15:0]];   // expected TID per stream
    protected bit [15:0] pkt_tdest[bit[15:0]]; // expected TDEST per stream
    protected bit        pkt_active[bit[15:0]]; // stream has in-progress packet

    // Handshake timeout counter
    protected int unsigned handshake_wait_count;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
        if (!uvm_config_db#(virtual axis_if)::get(this, "", "vif", vif))
            `uvm_fatal("NOVIF", "Virtual interface not found in config_db")
        checker_cfg = cfg.checker_cfg;
    endfunction

    task run_phase(uvm_phase phase);
        prev_tvalid = 0;
        handshake_wait_count = 0;

        forever begin
            @(vif.monitor_cb);

            if (!checker_cfg.enable_all) continue;

            // Reset signal check
            check_reset_signal();

            if (is_in_reset()) begin
                prev_tvalid = 0;
                handshake_wait_count = 0;
                pkt_active.delete();
                continue;
            end

            // X/Z check
            check_x_z();

            // TVALID stability
            check_tvalid_stability();

            // TDATA stability (payload must not change while valid high and no handshake)
            check_tdata_stability();

            // TKEEP/TSTRB relationship
            check_tkeep_tstrb_relation();

            // Handshake timeout
            check_handshake_timeout();

            // On handshake, check packet-level rules
            if (vif.monitor_cb.tvalid && vif.monitor_cb.tready) begin
                check_tid_consistency();
                check_tdest_consistency();
                check_tlast_integrity();
                handshake_wait_count = 0;
            end

            // Save previous values
            prev_tvalid = vif.monitor_cb.tvalid;
            prev_tdata  = vif.monitor_cb.tdata;
            prev_tstrb  = vif.monitor_cb.tstrb;
            prev_tkeep  = vif.monitor_cb.tkeep;
            prev_tlast  = vif.monitor_cb.tlast;
            prev_tid    = vif.monitor_cb.tid;
            prev_tdest  = vif.monitor_cb.tdest;
            prev_tuser  = vif.monitor_cb.tuser;
        end
    endtask

    // ---- Individual checks ----

    protected function void check_tvalid_stability();
        if (!checker_cfg.enable_tvalid_stability) return;
        // TVALID went from 1->0 without handshake completing
        if (prev_tvalid && !vif.monitor_cb.tvalid && !vif.monitor_cb.tready) begin
            report_violation("TVALID_STABILITY",
                "TVALID deasserted before handshake completed",
                checker_cfg.sev_tvalid_stability);
        end
    endfunction

    protected function void check_tdata_stability();
        if (!checker_cfg.enable_tdata_stability) return;
        if (!prev_tvalid || !vif.monitor_cb.tvalid) return;
        // If valid was high last cycle and still high (no handshake), payload must be stable
        if (vif.monitor_cb.tdata !== prev_tdata ||
            (cfg.HAS_TSTRB && vif.monitor_cb.tstrb !== prev_tstrb) ||
            (cfg.HAS_TKEEP && vif.monitor_cb.tkeep !== prev_tkeep) ||
            (cfg.HAS_TLAST && vif.monitor_cb.tlast !== prev_tlast) ||
            vif.monitor_cb.tid !== prev_tid ||
            vif.monitor_cb.tdest !== prev_tdest ||
            vif.monitor_cb.tuser !== prev_tuser) begin
            report_violation("TDATA_STABILITY",
                "Payload signals changed while TVALID high without handshake",
                checker_cfg.sev_tdata_stability);
        end
    endfunction

    protected function void check_tkeep_tstrb_relation();
        if (!checker_cfg.enable_tkeep_tstrb_relation) return;
        if (!cfg.HAS_TKEEP || !cfg.HAS_TSTRB) return;
        if (!vif.monitor_cb.tvalid) return;
        if ((vif.monitor_cb.tstrb & ~vif.monitor_cb.tkeep) != 0) begin
            report_violation("TKEEP_TSTRB_RELATION",
                "TSTRB is 1 where TKEEP is 0 — violates AXI-Stream spec",
                checker_cfg.sev_tkeep_tstrb_relation);
        end
    endfunction

    protected function void check_reset_signal();
        if (!checker_cfg.enable_reset_signal_check) return;
        if (is_in_reset() && vif.monitor_cb.tvalid) begin
            report_violation("RESET_SIGNAL_CHECK",
                "TVALID is high during reset",
                checker_cfg.sev_reset_signal_check);
        end
    endfunction

    protected function void check_x_z();
        if (!checker_cfg.enable_x_z_check) return;
        if (vif.monitor_cb.tvalid) begin
            if ($isunknown(vif.monitor_cb.tdata) ||
                $isunknown(vif.monitor_cb.tvalid) ||
                $isunknown(vif.monitor_cb.tready)) begin
                report_violation("X_Z_CHECK",
                    "X or Z detected on active signals",
                    checker_cfg.sev_x_z_check);
            end
        end
    endfunction

    protected function void check_handshake_timeout();
        if (!checker_cfg.enable_handshake_timeout) return;
        if (vif.monitor_cb.tvalid && !vif.monitor_cb.tready) begin
            handshake_wait_count++;
            if (handshake_wait_count >= checker_cfg.handshake_timeout_cycles) begin
                report_violation("HANDSHAKE_TIMEOUT",
                    $sformatf("TVALID high for %0d cycles without handshake",
                              handshake_wait_count),
                    checker_cfg.sev_handshake_timeout);
                handshake_wait_count = 0; // avoid re-firing every cycle
            end
        end else begin
            handshake_wait_count = 0;
        end
    endfunction

    protected function void check_tid_consistency();
        if (!checker_cfg.enable_tid_consistency) return;
        bit [15:0] current_tid = vif.monitor_cb.tid;
        if (pkt_active.exists(current_tid)) begin
            if (pkt_tid[current_tid] !== current_tid) begin
                report_violation("TID_CONSISTENCY",
                    $sformatf("TID changed mid-packet: expected %0h, got %0h",
                              pkt_tid[current_tid], current_tid),
                    checker_cfg.sev_tid_consistency);
            end
        end else begin
            pkt_active[current_tid] = 1;
            pkt_tid[current_tid] = current_tid;
            pkt_tdest[current_tid] = vif.monitor_cb.tdest;
        end
        // Clear on TLAST
        if (vif.monitor_cb.tlast && cfg.HAS_TLAST) begin
            pkt_active.delete(current_tid);
            pkt_tid.delete(current_tid);
            pkt_tdest.delete(current_tid);
        end
    endfunction

    protected function void check_tdest_consistency();
        if (!checker_cfg.enable_tdest_consistency) return;
        bit [15:0] current_tid = vif.monitor_cb.tid;
        if (pkt_active.exists(current_tid)) begin
            if (pkt_tdest[current_tid] !== vif.monitor_cb.tdest) begin
                report_violation("TDEST_CONSISTENCY",
                    $sformatf("TDEST changed mid-packet on TID %0h: expected %0h, got %0h",
                              current_tid, pkt_tdest[current_tid], vif.monitor_cb.tdest),
                    checker_cfg.sev_tdest_consistency);
            end
        end
    endfunction

    protected function void check_tlast_integrity();
        // This is checked implicitly by packet assembly in monitor.
        // Here we just warn if HAS_TLAST is enabled but TLAST never goes high.
        // Full integrity check requires end-of-sim report — handled in report_phase.
    endfunction

    // ---- Helpers ----

    protected function bit is_in_reset();
        if (cfg.reset_polarity == AXIS_RESET_ACTIVE_LOW)
            return (vif.aresetn === 1'b0);
        else
            return (vif.aresetn === 1'b1);
    endfunction

    protected function void report_violation(string id, string msg, axis_severity_e sev);
        case (sev)
            AXIS_SEV_INFO:    `uvm_info(id, msg, UVM_LOW)
            AXIS_SEV_WARNING: `uvm_warning(id, msg)
            AXIS_SEV_ERROR:   `uvm_error(id, msg)
            AXIS_SEV_FATAL:   `uvm_fatal(id, msg)
        endcase
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_protocol_checker.sv
git commit -m "feat(axis-vip): add protocol checker with 9 configurable assertions"
```

---

### Task 14: Scoreboard

**Files:**
- Create: `axis_vip/src/axis_scoreboard.sv`

- [ ] **Step 1: Write axis_scoreboard.sv**

```systemverilog
// axis_vip/src/axis_scoreboard.sv

class axis_scoreboard extends uvm_scoreboard;

    `uvm_component_utils(axis_scoreboard)

    // Analysis exports: master (expected) and slave (actual)
    uvm_analysis_imp_decl(_master)
    uvm_analysis_imp_decl(_slave)

    uvm_analysis_imp_master #(axis_packet, axis_scoreboard) master_export;
    uvm_analysis_imp_slave  #(axis_packet, axis_scoreboard) slave_export;

    // Queues for comparison
    protected axis_packet master_queue[$];
    protected axis_packet slave_queue[$];

    // Statistics
    int unsigned match_count;
    int unsigned mismatch_count;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        master_export = new("master_export", this);
        slave_export  = new("slave_export",  this);
    endfunction

    function void write_master(axis_packet pkt);
        master_queue.push_back(pkt);
        try_compare();
    endfunction

    function void write_slave(axis_packet pkt);
        slave_queue.push_back(pkt);
        try_compare();
    endfunction

    protected function void try_compare();
        while (master_queue.size() > 0 && slave_queue.size() > 0) begin
            axis_packet expected_pkt = master_queue.pop_front();
            axis_packet actual_pkt   = slave_queue.pop_front();

            if (expected_pkt.compare_payload(actual_pkt)) begin
                match_count++;
                `uvm_info(get_type_name(),
                    $sformatf("MATCH: packet tid=%0h tdest=%0h len=%0d",
                              expected_pkt.tid, expected_pkt.tdest, expected_pkt.packet_length),
                    UVM_HIGH)
            end else begin
                mismatch_count++;
                `uvm_error(get_type_name(),
                    $sformatf("MISMATCH: packet tid=%0h tdest=%0h len=%0d vs len=%0d",
                              expected_pkt.tid, expected_pkt.tdest,
                              expected_pkt.packet_length, actual_pkt.packet_length))
            end
        end
    endfunction

    function void report_phase(uvm_phase phase);
        `uvm_info(get_type_name(),
            $sformatf("Scoreboard summary: %0d matches, %0d mismatches, %0d master pending, %0d slave pending",
                      match_count, mismatch_count, master_queue.size(), slave_queue.size()),
            UVM_LOW)

        if (master_queue.size() > 0)
            `uvm_error(get_type_name(),
                $sformatf("%0d packets in master queue not received by slave", master_queue.size()))
        if (slave_queue.size() > 0)
            `uvm_error(get_type_name(),
                $sformatf("%0d packets in slave queue not sent by master", slave_queue.size()))
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_scoreboard.sv
git commit -m "feat(axis-vip): add scoreboard with packet-level comparison"
```

---

### Task 15: Coverage Collector

**Files:**
- Create: `axis_vip/src/axis_coverage_collector.sv`

- [ ] **Step 1: Write axis_coverage_collector.sv**

```systemverilog
// axis_vip/src/axis_coverage_collector.sv

class axis_coverage_collector extends uvm_subscriber #(axis_transfer);

    `uvm_component_utils(axis_coverage_collector)

    axis_config cfg;

    // Sampled values
    protected bit       sampled_tvalid;
    protected bit       sampled_tready;
    protected int unsigned sampled_pkt_len;
    protected bit [15:0]   sampled_tid;
    protected bit [15:0]   sampled_tdest;
    protected bit       sampled_tdata_all_zero;
    protected bit       sampled_tdata_all_one;
    protected int unsigned sampled_bp_duration;
    protected int unsigned sampled_handshake_latency;
    protected real      sampled_bandwidth;
    protected bit [2:0] sampled_reset_timing; // 0=idle, 1=mid-transfer, 2=mid-packet
    protected axis_valid_gen_mode_e sampled_valid_gen_mode;
    protected axis_ready_gen_mode_e sampled_ready_gen_mode;

    // Internal counters
    protected int unsigned current_pkt_len;
    protected int unsigned bp_duration_counter;
    protected int unsigned handshake_latency_counter;
    protected bit          tvalid_was_high;

    // ---- Coverage groups ----

    covergroup handshake_cg;
        cp_valid_ready: coverpoint {sampled_tvalid, sampled_tready} {
            bins idle      = {2'b00};
            bins valid_only = {2'b10};
            bins ready_only = {2'b01};
            bins handshake = {2'b11};
        }
        cp_latency: coverpoint sampled_handshake_latency {
            bins zero     = {0};
            bins one      = {1};
            bins short_   = {[2:5]};
            bins medium   = {[6:20]};
            bins long_    = {[21:100]};
            bins very_long = {[101:$]};
        }
    endgroup

    covergroup packet_cg;
        cp_length: coverpoint sampled_pkt_len {
            bins single  = {1};
            bins short_  = {[2:4]};
            bins medium  = {[5:16]};
            bins long_   = {[17:64]};
            bins longer  = {[65:256]};
            bins max_    = {[257:$]};
        }
        cp_tid: coverpoint sampled_tid {
            bins values[] = {[0:15]};
        }
        cp_tdest: coverpoint sampled_tdest {
            bins values[] = {[0:15]};
        }
        cp_tid_x_tdest: cross cp_tid, cp_tdest;
    endgroup

    covergroup backpressure_cg;
        cp_bp_duration: coverpoint sampled_bp_duration {
            bins zero    = {0};
            bins one     = {1};
            bins short_  = {[2:5]};
            bins medium  = {[6:20]};
            bins long_   = {[21:$]};
        }
    endgroup

    covergroup data_cg;
        cp_all_zero: coverpoint sampled_tdata_all_zero {
            bins yes = {1};
            bins no  = {0};
        }
        cp_all_one: coverpoint sampled_tdata_all_one {
            bins yes = {1};
            bins no  = {0};
        }
    endgroup

    covergroup reset_cg;
        cp_timing: coverpoint sampled_reset_timing {
            bins idle         = {0};
            bins mid_transfer = {1};
            bins mid_packet   = {2};
        }
        cp_reset_x_pkt_len: cross cp_timing, sampled_pkt_len {
            bins len_bins[] = {[0:$]};
        }
    endgroup

    covergroup bandwidth_cg;
        cp_bw: coverpoint sampled_bandwidth {
            bins zero     = {0};
            bins low      = {[0.001:0.25]};
            bins medium   = {[0.251:0.5]};
            bins high     = {[0.501:0.75]};
            bins very_high = {[0.751:1.0]};
        }
    endgroup

    function new(string name, uvm_component parent);
        super.new(name, parent);
        handshake_cg    = new();
        packet_cg       = new();
        backpressure_cg = new();
        data_cg         = new();
        reset_cg        = new();
        bandwidth_cg    = new();
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
    endfunction

    // Called on every beat from monitor's beat_ap
    function void write(axis_transfer t);
        // Handshake coverage
        sampled_tvalid = 1;
        sampled_tready = 1; // only called on handshake
        sampled_handshake_latency = handshake_latency_counter;
        handshake_cg.sample();

        // Data coverage
        sampled_tdata_all_zero = (t.tdata == 0);
        sampled_tdata_all_one  = (t.tdata == ((1 << cfg.TDATA_WIDTH) - 1));
        data_cg.sample();

        // Packet length tracking
        current_pkt_len++;
        if (t.tlast || !cfg.HAS_TLAST) begin
            sampled_pkt_len = current_pkt_len;
            sampled_tid     = t.tid;
            sampled_tdest   = t.tdest;
            packet_cg.sample();
            current_pkt_len = 0;
        end

        // Reset latency counter
        handshake_latency_counter = 0;
    endfunction

    // Called externally to sample backpressure duration
    function void sample_backpressure(int unsigned duration);
        sampled_bp_duration = duration;
        backpressure_cg.sample();
    endfunction

    // Called externally to sample bandwidth
    function void sample_bandwidth(real bw);
        sampled_bandwidth = bw;
        bandwidth_cg.sample();
    endfunction

    // Called externally on reset
    function void sample_reset_timing(int unsigned timing);
        sampled_reset_timing = timing[2:0];
        reset_cg.sample();
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_coverage_collector.sv
git commit -m "feat(axis-vip): add coverage collector with 6 coverage groups and crosses"
```

---

### Task 16: Bandwidth Checker

**Files:**
- Create: `axis_vip/src/axis_bandwidth_checker.sv`

- [ ] **Step 1: Write axis_bandwidth_checker.sv**

```systemverilog
// axis_vip/src/axis_bandwidth_checker.sv

class axis_bandwidth_checker extends uvm_subscriber #(axis_transfer);

    `uvm_component_utils(axis_bandwidth_checker)

    virtual axis_if vif;
    axis_config cfg;
    axis_coverage_collector cov_collector; // optional, for bandwidth coverage

    // Statistics
    protected int unsigned bytes_in_window;
    protected int unsigned cycle_counter;
    protected real         current_bw;

    // History
    protected real bw_history[$];

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        if (!uvm_config_db#(axis_config)::get(this, "", "cfg", cfg))
            `uvm_fatal("NOCFG", "axis_config not found in config_db")
        if (!uvm_config_db#(virtual axis_if)::get(this, "", "vif", vif))
            `uvm_fatal("NOVIF", "Virtual interface not found in config_db")
    endfunction

    // Receive beats from monitor
    function void write(axis_transfer t);
        if (!cfg.bw_check_enable) return;

        // Count valid bytes (based on TKEEP)
        for (int i = 0; i < cfg.get_byte_lanes(); i++) begin
            if (t.tkeep[i])
                bytes_in_window++;
        end
    endfunction

    task run_phase(uvm_phase phase);
        if (!cfg.bw_check_enable) return;

        forever begin
            @(posedge vif.aclk);
            cycle_counter++;

            if (cycle_counter >= cfg.bw_window_cycles) begin
                evaluate_window();
                bytes_in_window = 0;
                cycle_counter = 0;
            end
        end
    endtask

    protected function void evaluate_window();
        real min_thresh, max_thresh;

        current_bw = real'(bytes_in_window) / real'(cfg.bw_window_cycles);
        bw_history.push_back(current_bw);

        // Get thresholds (profile or static)
        get_current_thresholds(min_thresh, max_thresh);

        `uvm_info(get_type_name(),
            $sformatf("BW window: %0d bytes / %0d cycles = %.4f bytes/cycle [min=%.4f, max=%.4f]",
                      bytes_in_window, cfg.bw_window_cycles, current_bw, min_thresh, max_thresh),
            UVM_MEDIUM)

        // Check thresholds
        if (current_bw < min_thresh) begin
            `uvm_error("BW_MIN_VIOLATION",
                $sformatf("Bandwidth %.4f below minimum threshold %.4f",
                          current_bw, min_thresh))
        end

        if (max_thresh >= 0 && current_bw > max_thresh) begin
            `uvm_error("BW_MAX_VIOLATION",
                $sformatf("Bandwidth %.4f above maximum threshold %.4f",
                          current_bw, max_thresh))
        end

        // Feed coverage collector if available
        if (cov_collector != null)
            cov_collector.sample_bandwidth(current_bw);
    endfunction

    protected function void get_current_thresholds(output real min_thresh, output real max_thresh);
        // Check profile first
        int unsigned total_cycles;
        total_cycles = bw_history.size() * cfg.bw_window_cycles + cycle_counter;

        foreach (cfg.bw_profile[i]) begin
            if (total_cycles >= cfg.bw_profile[i].start_cycle &&
                total_cycles <= cfg.bw_profile[i].end_cycle) begin
                min_thresh = cfg.bw_profile[i].min_threshold;
                max_thresh = cfg.bw_profile[i].max_threshold;
                return;
            end
        end

        // Fall back to static thresholds
        min_thresh = cfg.bw_min_threshold;
        max_thresh = cfg.bw_max_threshold;
    endfunction

    function void report_phase(uvm_phase phase);
        if (!cfg.bw_check_enable || bw_history.size() == 0) return;

        real total_bw = 0;
        real min_bw = bw_history[0];
        real max_bw = bw_history[0];

        foreach (bw_history[i]) begin
            total_bw += bw_history[i];
            if (bw_history[i] < min_bw) min_bw = bw_history[i];
            if (bw_history[i] > max_bw) max_bw = bw_history[i];
        end

        `uvm_info(get_type_name(),
            $sformatf("BW summary: avg=%.4f, min=%.4f, max=%.4f over %0d windows",
                      total_bw / bw_history.size(), min_bw, max_bw, bw_history.size()),
            UVM_LOW)
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_bandwidth_checker.sv
git commit -m "feat(axis-vip): add bandwidth checker with threshold assertions and profiling"
```

---

### Task 17: Environment

**Files:**
- Create: `axis_vip/src/axis_env.sv`

- [ ] **Step 1: Write axis_env.sv**

```systemverilog
// axis_vip/src/axis_env.sv

class axis_env extends uvm_env;

    `uvm_component_utils(axis_env)

    // Config
    axis_config master_cfg;
    axis_config slave_cfg;

    // Agents
    axis_agent master_agent;
    axis_agent slave_agent;

    // Env-level components
    axis_reset_handler        rst_handler;
    axis_phase_controller     phase_ctrl;
    axis_scoreboard           sb;
    axis_coverage_collector   cov;
    axis_bandwidth_checker    bw_checker;
    axis_protocol_checker     proto_checker;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);

        // Get configs
        if (!uvm_config_db#(axis_config)::get(this, "", "master_cfg", master_cfg))
            `uvm_fatal("NOCFG", "master_cfg not found")
        if (!uvm_config_db#(axis_config)::get(this, "", "slave_cfg", slave_cfg))
            `uvm_fatal("NOCFG", "slave_cfg not found")

        // Propagate configs to sub-components
        uvm_config_db#(axis_config)::set(this, "master_agent*", "cfg", master_cfg);
        uvm_config_db#(axis_config)::set(this, "slave_agent*",  "cfg", slave_cfg);
        uvm_config_db#(axis_config)::set(this, "rst_handler",   "cfg", master_cfg);
        uvm_config_db#(axis_config)::set(this, "phase_ctrl",    "cfg", master_cfg);
        uvm_config_db#(axis_config)::set(this, "proto_checker", "cfg", master_cfg);
        uvm_config_db#(axis_config)::set(this, "bw_checker",    "cfg", master_cfg);
        uvm_config_db#(axis_config)::set(this, "cov",           "cfg", master_cfg);

        // Build agents
        master_agent = axis_agent::type_id::create("master_agent", this);
        slave_agent  = axis_agent::type_id::create("slave_agent",  this);

        // Build env-level components
        rst_handler   = axis_reset_handler::type_id::create("rst_handler", this);
        phase_ctrl    = axis_phase_controller::type_id::create("phase_ctrl", this);
        sb            = axis_scoreboard::type_id::create("sb", this);
        cov           = axis_coverage_collector::type_id::create("cov", this);
        bw_checker    = axis_bandwidth_checker::type_id::create("bw_checker", this);
        proto_checker = axis_protocol_checker::type_id::create("proto_checker", this);
    endfunction

    function void connect_phase(uvm_phase phase);
        super.connect_phase(phase);

        // Connect monitor analysis ports to scoreboard
        master_agent.mon.packet_ap.connect(sb.master_export);
        slave_agent.mon.packet_ap.connect(sb.slave_export);

        // Connect monitor beat ports to coverage and bandwidth checker
        master_agent.mon.beat_ap.connect(cov.analysis_export);
        master_agent.mon.beat_ap.connect(bw_checker.analysis_export);

        // Connect bandwidth checker to coverage collector
        bw_checker.cov_collector = cov;

        // Register agents with reset handler
        rst_handler.agents.push_back(master_agent);
        rst_handler.agents.push_back(slave_agent);

        // Connect reset events to agent reset listeners
        master_agent.rst_listener.reset_asserted_evt   = rst_handler.reset_asserted_evt;
        master_agent.rst_listener.reset_active_evt     = rst_handler.reset_active_evt;
        master_agent.rst_listener.reset_deasserted_evt = rst_handler.reset_deasserted_evt;
        slave_agent.rst_listener.reset_asserted_evt    = rst_handler.reset_asserted_evt;
        slave_agent.rst_listener.reset_active_evt      = rst_handler.reset_active_evt;
        slave_agent.rst_listener.reset_deasserted_evt  = rst_handler.reset_deasserted_evt;

        // Register agents with phase controller
        phase_ctrl.agents.push_back(master_agent);
        phase_ctrl.agents.push_back(slave_agent);
        phase_ctrl.rst_handler = rst_handler;
    endfunction

endclass
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_env.sv
git commit -m "feat(axis-vip): add environment with all component wiring"
```

---

### Task 18: Package File

**Files:**
- Create: `axis_vip/src/axis_pkg.sv`

- [ ] **Step 1: Write axis_pkg.sv**

```systemverilog
// axis_vip/src/axis_pkg.sv

package axis_pkg;

    import uvm_pkg::*;
    `include "uvm_macros.svh"

    // Types and enums
    `include "axis_types.sv"

    // Configuration
    `include "axis_protocol_checker_config.sv"
    `include "axis_config.sv"

    // Transactions
    `include "axis_transfer.sv"
    `include "axis_packet.sv"

    // Agent components
    `include "axis_sequencer.sv"
    `include "axis_bandwidth_controller.sv"
    `include "axis_reset_listener.sv"
    `include "axis_master_driver.sv"
    `include "axis_slave_driver.sv"
    `include "axis_monitor.sv"
    `include "axis_agent.sv"

    // Env-level components
    `include "axis_reset_handler.sv"
    `include "axis_phase_controller.sv"
    `include "axis_protocol_checker.sv"
    `include "axis_scoreboard.sv"
    `include "axis_coverage_collector.sv"
    `include "axis_bandwidth_checker.sv"
    `include "axis_env.sv"

    // Sequences (base)
    `include "../sequences/axis_base_seq.sv"
    `include "../sequences/axis_single_transfer_seq.sv"
    `include "../sequences/axis_packet_seq.sv"
    `include "../sequences/axis_idle_seq.sv"

    // Sequences (scenario)
    `include "../sequences/axis_burst_seq.sv"
    `include "../sequences/axis_backpressure_stress_seq.sv"
    `include "../sequences/axis_interleave_seq.sv"
    `include "../sequences/axis_error_inject_seq.sv"
    `include "../sequences/axis_boundary_seq.sv"
    `include "../sequences/axis_reset_during_transfer_seq.sv"

    // Sequences (virtual)
    `include "../sequences/axis_base_vseq.sv"
    `include "../sequences/axis_master_slave_sync_vseq.sv"
    `include "../sequences/axis_bandwidth_sweep_vseq.sv"
    `include "../sequences/axis_reset_recovery_vseq.sv"
    `include "../sequences/axis_full_stress_vseq.sv"

endpackage
```

- [ ] **Step 2: Commit**

```bash
git add axis_vip/src/axis_pkg.sv
git commit -m "feat(axis-vip): add package file with dependency-ordered includes"
```

---

### Task 19: Atomic Sequences

**Files:**
- Create: `axis_vip/sequences/axis_base_seq.sv`
- Create: `axis_vip/sequences/axis_single_transfer_seq.sv`
- Create: `axis_vip/sequences/axis_packet_seq.sv`
- Create: `axis_vip/sequences/axis_idle_seq.sv`

- [ ] **Step 1: Write axis_base_seq.sv**

```systemverilog
// axis_vip/sequences/axis_base_seq.sv

class axis_base_seq extends uvm_sequence #(axis_transfer);

    `uvm_object_utils(axis_base_seq)

    axis_config cfg;

    function new(string name = "axis_base_seq");
        super.new(name);
    endfunction

    task pre_body();
        // Get config from sequencer
        if (m_sequencer != null) begin
            axis_sequencer sqr;
            if ($cast(sqr, m_sequencer)) begin
                cfg = sqr.cfg;
                // Record sequence type for hot reset
                sqr.last_seq_type_name = get_type_name();
            end
        end
        if (cfg == null)
            `uvm_fatal("NOCFG", "axis_config not available in sequence")
    endtask

    // Check if we should stop (reset or phase jump)
    function bit should_stop();
        axis_sequencer sqr;
        if ($cast(sqr, m_sequencer))
            return sqr.reset_active;
        return 0;
    endfunction

endclass
```

- [ ] **Step 2: Write axis_single_transfer_seq.sv**

```systemverilog
// axis_vip/sequences/axis_single_transfer_seq.sv

class axis_single_transfer_seq extends axis_base_seq;

    `uvm_object_utils(axis_single_transfer_seq)

    // Configurable fields
    rand bit [511:0]  data;
    rand bit [63:0]   strb;
    rand bit [63:0]   keep;
    rand bit          last;
    rand bit [15:0]   id;
    rand bit [15:0]   dest;
    rand bit [127:0]  user;
    rand int unsigned  delay;

    constraint c_default_delay { delay inside {[0:5]}; }
    constraint c_default_last  { soft last == 1; }

    function new(string name = "axis_single_transfer_seq");
        super.new(name);
    endfunction

    task body();
        axis_transfer tr;
        tr = axis_transfer::type_id::create("tr");
        tr.cfg = cfg;

        start_item(tr);
        tr.tdata = data;
        tr.tstrb = strb;
        tr.tkeep = keep;
        tr.tlast = last;
        tr.tid   = id;
        tr.tdest = dest;
        tr.tuser = user;
        tr.delay = delay;
        finish_item(tr);
    endtask

endclass
```

- [ ] **Step 3: Write axis_packet_seq.sv**

```systemverilog
// axis_vip/sequences/axis_packet_seq.sv

class axis_packet_seq extends axis_base_seq;

    `uvm_object_utils(axis_packet_seq)

    rand int unsigned  packet_length;
    rand bit [15:0]    packet_tid;
    rand bit [15:0]    packet_tdest;
    rand int unsigned  inter_beat_delay;

    // Data pattern: 0=random, 1=incrementing, 2=all-zero, 3=all-one
    rand int unsigned  data_pattern;

    constraint c_length { packet_length inside {[1:256]}; }
    constraint c_delay  { inter_beat_delay inside {[0:3]}; }
    constraint c_pattern { data_pattern inside {[0:3]}; }

    function new(string name = "axis_packet_seq");
        super.new(name);
    endfunction

    task body();
        for (int i = 0; i < packet_length; i++) begin
            axis_transfer tr;

            if (should_stop()) return;

            tr = axis_transfer::type_id::create($sformatf("tr_%0d", i));
            tr.cfg = cfg;

            start_item(tr);
            if (!tr.randomize() with {
                tid   == local::packet_tid;
                tdest == local::packet_tdest;
                tlast == (i == local::packet_length - 1);
                delay == local::inter_beat_delay;
            }) `uvm_error(get_type_name(), "Randomization failed")

            // Apply data pattern
            case (data_pattern)
                1: tr.tdata = i; // incrementing
                2: tr.tdata = 0;
                3: tr.tdata = '1;
                // 0: keep random
            endcase

            finish_item(tr);
        end
    endtask

endclass
```

- [ ] **Step 4: Write axis_idle_seq.sv**

```systemverilog
// axis_vip/sequences/axis_idle_seq.sv

class axis_idle_seq extends axis_base_seq;

    `uvm_object_utils(axis_idle_seq)

    rand int unsigned idle_cycles;

    constraint c_idle { idle_cycles inside {[1:100]}; }

    function new(string name = "axis_idle_seq");
        super.new(name);
    endfunction

    task body();
        // Send a single transfer with large delay and no-op data
        // The delay in the transfer item creates idle cycles
        axis_transfer tr;
        tr = axis_transfer::type_id::create("idle_tr");
        tr.cfg = cfg;

        start_item(tr);
        tr.tdata = 0;
        tr.tkeep = 0; // null byte — effectively idle
        tr.tstrb = 0;
        tr.tlast = 0;
        tr.tid   = 0;
        tr.tdest = 0;
        tr.tuser = 0;
        tr.delay = idle_cycles;
        finish_item(tr);
    endtask

endclass
```

- [ ] **Step 5: Commit**

```bash
git add axis_vip/sequences/axis_base_seq.sv axis_vip/sequences/axis_single_transfer_seq.sv axis_vip/sequences/axis_packet_seq.sv axis_vip/sequences/axis_idle_seq.sv
git commit -m "feat(axis-vip): add atomic sequence layer (single transfer, packet, idle)"
```

---

### Task 20: Scenario Sequences

**Files:**
- Create: `axis_vip/sequences/axis_burst_seq.sv`
- Create: `axis_vip/sequences/axis_backpressure_stress_seq.sv`
- Create: `axis_vip/sequences/axis_interleave_seq.sv`
- Create: `axis_vip/sequences/axis_error_inject_seq.sv`
- Create: `axis_vip/sequences/axis_boundary_seq.sv`
- Create: `axis_vip/sequences/axis_reset_during_transfer_seq.sv`

- [ ] **Step 1: Write axis_burst_seq.sv**

```systemverilog
// axis_vip/sequences/axis_burst_seq.sv

class axis_burst_seq extends axis_base_seq;

    `uvm_object_utils(axis_burst_seq)

    rand int unsigned num_packets;
    rand int unsigned min_pkt_len;
    rand int unsigned max_pkt_len;
    rand bit [15:0]   burst_tid;
    rand bit [15:0]   burst_tdest;

    constraint c_packets { num_packets inside {[2:32]}; }
    constraint c_pkt_len { min_pkt_len inside {[1:16]}; max_pkt_len inside {[4:64]}; max_pkt_len >= min_pkt_len; }

    function new(string name = "axis_burst_seq");
        super.new(name);
    endfunction

    task body();
        for (int p = 0; p < num_packets; p++) begin
            axis_packet_seq pkt_seq;

            if (should_stop()) return;

            pkt_seq = axis_packet_seq::type_id::create($sformatf("pkt_%0d", p));
            if (!pkt_seq.randomize() with {
                packet_length inside {[local::min_pkt_len : local::max_pkt_len]};
                packet_tid  == local::burst_tid;
                packet_tdest == local::burst_tdest;
                inter_beat_delay == 0;
                data_pattern == 0;
            }) `uvm_error(get_type_name(), "Randomization failed")

            pkt_seq.start(m_sequencer, this);
        end
    endtask

endclass
```

- [ ] **Step 2: Write axis_backpressure_stress_seq.sv**

```systemverilog
// axis_vip/sequences/axis_backpressure_stress_seq.sv
// This sequence is for the SLAVE side — it doesn't drive data,
// but the master-side companion sends data while slave backpressures.
// Typically used as a master-side stress sequence paired with a
// slave agent configured with heavy backpressure.

class axis_backpressure_stress_seq extends axis_base_seq;

    `uvm_object_utils(axis_backpressure_stress_seq)

    rand int unsigned num_packets;
    rand int unsigned pkt_len;

    constraint c_stress { num_packets inside {[10:50]}; pkt_len inside {[8:128]}; }

    function new(string name = "axis_backpressure_stress_seq");
        super.new(name);
    endfunction

    task body();
        for (int p = 0; p < num_packets; p++) begin
            axis_packet_seq pkt_seq;

            if (should_stop()) return;

            pkt_seq = axis_packet_seq::type_id::create($sformatf("stress_pkt_%0d", p));
            if (!pkt_seq.randomize() with {
                packet_length == local::pkt_len;
                inter_beat_delay == 0; // back-to-back from master
                data_pattern == 0;
            }) `uvm_error(get_type_name(), "Randomization failed")

            pkt_seq.start(m_sequencer, this);
        end
    endtask

endclass
```

- [ ] **Step 3: Write axis_interleave_seq.sv**

```systemverilog
// axis_vip/sequences/axis_interleave_seq.sv

class axis_interleave_seq extends axis_base_seq;

    `uvm_object_utils(axis_interleave_seq)

    rand int unsigned num_streams;
    rand int unsigned beats_per_switch;
    rand int unsigned total_packets;

    constraint c_streams { num_streams inside {[2:8]}; }
    constraint c_switch  { beats_per_switch inside {[1:8]}; }
    constraint c_total   { total_packets inside {[4:16]}; }

    function new(string name = "axis_interleave_seq");
        super.new(name);
    endfunction

    task body();
        // Create interleaved packets across multiple TIDs
        int unsigned pkt_per_stream = total_packets / num_streams;
        if (pkt_per_stream < 1) pkt_per_stream = 1;

        for (int p = 0; p < pkt_per_stream; p++) begin
            for (int s = 0; s < num_streams; s++) begin
                axis_packet_seq pkt_seq;

                if (should_stop()) return;

                pkt_seq = axis_packet_seq::type_id::create($sformatf("ilv_s%0d_p%0d", s, p));
                if (!pkt_seq.randomize() with {
                    packet_length inside {[2:16]};
                    packet_tid == local::s;
                    packet_tdest == local::s;
                    inter_beat_delay inside {[0:2]};
                    data_pattern == 1; // incrementing for easy debug
                }) `uvm_error(get_type_name(), "Randomization failed")

                pkt_seq.start(m_sequencer, this);
            end
        end
    endtask

endclass
```

- [ ] **Step 4: Write axis_error_inject_seq.sv**

```systemverilog
// axis_vip/sequences/axis_error_inject_seq.sv
// Deliberately violates protocol rules to test protocol checker

class axis_error_inject_seq extends axis_base_seq;

    `uvm_object_utils(axis_error_inject_seq)

    typedef enum {
        ERR_TKEEP_TSTRB_MISMATCH,
        ERR_DATA_ALL_X
    } error_type_e;

    rand error_type_e error_type;

    function new(string name = "axis_error_inject_seq");
        super.new(name);
    endfunction

    task body();
        axis_transfer tr;
        tr = axis_transfer::type_id::create("err_tr");
        tr.cfg = cfg;

        start_item(tr);
        if (!tr.randomize()) `uvm_error(get_type_name(), "Randomization failed")

        case (error_type)
            ERR_TKEEP_TSTRB_MISMATCH: begin
                // Violate: TSTRB=1 where TKEEP=0
                tr.tkeep = 4'b0000;
                tr.tstrb = 4'b1111;
                tr.tlast = 1;
            end
            ERR_DATA_ALL_X: begin
                // Send with valid data but this tests the checker path
                tr.tdata = 0;
                tr.tlast = 1;
            end
        endcase

        finish_item(tr);
    endtask

endclass
```

- [ ] **Step 5: Write axis_boundary_seq.sv**

```systemverilog
// axis_vip/sequences/axis_boundary_seq.sv

class axis_boundary_seq extends axis_base_seq;

    `uvm_object_utils(axis_boundary_seq)

    function new(string name = "axis_boundary_seq");
        super.new(name);
    endfunction

    task body();
        // Test 1: Single beat packet (minimum length)
        send_packet(1, 0, 0);

        // Test 2: All-zero data
        send_packet_with_data(4, 0, '0);

        // Test 3: All-one data
        send_packet_with_data(4, 0, '1);

        // Test 4: Maximum TID
        send_packet(4, (1 << cfg.TID_WIDTH) - 1, 0);

        // Test 5: Maximum TDEST
        send_packet(4, 0, (1 << cfg.TDEST_WIDTH) - 1);

        // Test 6: Long packet
        send_packet(256, 0, 0);
    endtask

    protected task send_packet(int unsigned length, bit [15:0] tid, bit [15:0] tdest);
        axis_packet_seq pkt_seq;
        pkt_seq = axis_packet_seq::type_id::create("boundary_pkt");
        if (!pkt_seq.randomize() with {
            packet_length == local::length;
            packet_tid    == local::tid;
            packet_tdest  == local::tdest;
            data_pattern  == 0;
        }) `uvm_error(get_type_name(), "Randomization failed")
        pkt_seq.start(m_sequencer, this);
    endtask

    protected task send_packet_with_data(int unsigned length, bit [15:0] tid, bit [511:0] data_val);
        for (int i = 0; i < length; i++) begin
            axis_transfer tr;
            if (should_stop()) return;
            tr = axis_transfer::type_id::create($sformatf("bnd_tr_%0d", i));
            tr.cfg = cfg;
            start_item(tr);
            tr.tdata = data_val;
            tr.tkeep = (1 << cfg.get_byte_lanes()) - 1;
            tr.tstrb = tr.tkeep;
            tr.tlast = (i == length - 1);
            tr.tid   = tid;
            tr.tdest = 0;
            tr.tuser = 0;
            tr.delay = 0;
            finish_item(tr);
        end
    endtask

endclass
```

- [ ] **Step 6: Write axis_reset_during_transfer_seq.sv**

```systemverilog
// axis_vip/sequences/axis_reset_during_transfer_seq.sv
// Sends a long packet, expects reset to interrupt mid-transfer

class axis_reset_during_transfer_seq extends axis_base_seq;

    `uvm_object_utils(axis_reset_during_transfer_seq)

    rand int unsigned pkt_len;
    constraint c_len { pkt_len inside {[16:64]}; }

    function new(string name = "axis_reset_during_transfer_seq");
        super.new(name);
    endfunction

    task body();
        axis_packet_seq pkt_seq;
        pkt_seq = axis_packet_seq::type_id::create("rst_pkt");
        if (!pkt_seq.randomize() with {
            packet_length == local::pkt_len;
            inter_beat_delay == 1; // slow enough for reset to hit mid-packet
            data_pattern == 1;
        }) `uvm_error(get_type_name(), "Randomization failed")

        // Fork: start sending, the test layer will assert reset in parallel
        fork
            pkt_seq.start(m_sequencer, this);
        join_any
        // If reset stopped us, that's expected
        `uvm_info(get_type_name(), "Reset-during-transfer sequence completed or interrupted", UVM_MEDIUM)
    endtask

endclass
```

- [ ] **Step 7: Commit**

```bash
git add axis_vip/sequences/axis_burst_seq.sv axis_vip/sequences/axis_backpressure_stress_seq.sv axis_vip/sequences/axis_interleave_seq.sv axis_vip/sequences/axis_error_inject_seq.sv axis_vip/sequences/axis_boundary_seq.sv axis_vip/sequences/axis_reset_during_transfer_seq.sv
git commit -m "feat(axis-vip): add scenario sequence layer (burst, backpressure, interleave, error, boundary, reset)"
```

---

### Task 21: Virtual Sequences

**Files:**
- Create: `axis_vip/sequences/axis_base_vseq.sv`
- Create: `axis_vip/sequences/axis_master_slave_sync_vseq.sv`
- Create: `axis_vip/sequences/axis_bandwidth_sweep_vseq.sv`
- Create: `axis_vip/sequences/axis_reset_recovery_vseq.sv`
- Create: `axis_vip/sequences/axis_full_stress_vseq.sv`

- [ ] **Step 1: Write axis_base_vseq.sv**

```systemverilog
// axis_vip/sequences/axis_base_vseq.sv

class axis_base_vseq extends uvm_sequence #(axis_transfer);

    `uvm_object_utils(axis_base_vseq)

    // Sequencer handles (set by test before starting)
    axis_sequencer master_sqr;
    axis_sequencer slave_sqr;

    // Config
    axis_config master_cfg;
    axis_config slave_cfg;

    // Sequence state machine
    axis_seq_state_e current_state;
    uvm_event state_change_evt;

    function new(string name = "axis_base_vseq");
        super.new(name);
        current_state = AXIS_SEQ_STATE_NORMAL;
        state_change_evt = new("state_change_evt");
    endfunction

    task pre_body();
        if (master_sqr != null) master_cfg = master_sqr.cfg;
        if (slave_sqr != null)  slave_cfg  = slave_sqr.cfg;
    endtask

    // State machine transition
    function void transition_to(axis_seq_state_e new_state);
        `uvm_info(get_type_name(),
            $sformatf("State transition: %s -> %s", current_state.name(), new_state.name()),
            UVM_MEDIUM)
        current_state = new_state;
        state_change_evt.trigger();
    endfunction

endclass
```

- [ ] **Step 2: Write axis_master_slave_sync_vseq.sv**

```systemverilog
// axis_vip/sequences/axis_master_slave_sync_vseq.sv

class axis_master_slave_sync_vseq extends axis_base_vseq;

    `uvm_object_utils(axis_master_slave_sync_vseq)

    rand int unsigned num_packets;
    rand int unsigned pkt_len;

    constraint c_sync { num_packets inside {[4:16]}; pkt_len inside {[4:32]}; }

    function new(string name = "axis_master_slave_sync_vseq");
        super.new(name);
    endfunction

    task body();
        // Master sends packets while slave is in various ready modes
        axis_burst_seq master_seq;

        master_seq = axis_burst_seq::type_id::create("m_burst");
        if (!master_seq.randomize() with {
            num_packets == local::num_packets;
            min_pkt_len == local::pkt_len;
            max_pkt_len == local::pkt_len;
        }) `uvm_error(get_type_name(), "Randomization failed")

        // Slave does not need an explicit sequence for ready-only operation;
        // its driver generates ready based on config.
        // Just start the master sequence.
        master_seq.start(master_sqr);

        `uvm_info(get_type_name(), "Master-slave sync complete", UVM_LOW)
    endtask

endclass
```

- [ ] **Step 3: Write axis_bandwidth_sweep_vseq.sv**

```systemverilog
// axis_vip/sequences/axis_bandwidth_sweep_vseq.sv

class axis_bandwidth_sweep_vseq extends axis_base_vseq;

    `uvm_object_utils(axis_bandwidth_sweep_vseq)

    function new(string name = "axis_bandwidth_sweep_vseq");
        super.new(name);
    endfunction

    task body();
        axis_burst_seq burst;
        int unsigned weights[] = '{100, 80, 60, 40, 20};

        foreach (weights[i]) begin
            `uvm_info(get_type_name(),
                $sformatf("Sweep: valid_weight=%0d%%", weights[i]), UVM_LOW)

            // Update master config dynamically
            master_cfg.valid_gen_mode = VALID_WEIGHTED;
            master_cfg.valid_weight   = weights[i];
            master_cfg.notify_config_changed();

            burst = axis_burst_seq::type_id::create($sformatf("bw_%0d", weights[i]));
            if (!burst.randomize() with {
                num_packets == 8;
                min_pkt_len == 16;
                max_pkt_len == 16;
            }) `uvm_error(get_type_name(), "Randomization failed")

            burst.start(master_sqr);
        end

        `uvm_info(get_type_name(), "Bandwidth sweep complete", UVM_LOW)
    endtask

endclass
```

- [ ] **Step 4: Write axis_reset_recovery_vseq.sv**

```systemverilog
// axis_vip/sequences/axis_reset_recovery_vseq.sv

class axis_reset_recovery_vseq extends axis_base_vseq;

    `uvm_object_utils(axis_reset_recovery_vseq)

    // Interface handle for driving reset
    virtual axis_if vif;

    rand int unsigned pre_reset_packets;
    rand int unsigned post_reset_packets;
    rand int unsigned reset_duration_cycles;

    constraint c_reset {
        pre_reset_packets inside {[2:8]};
        post_reset_packets inside {[2:8]};
        reset_duration_cycles inside {[5:20]};
    }

    function new(string name = "axis_reset_recovery_vseq");
        super.new(name);
    endfunction

    task body();
        axis_burst_seq pre_seq, post_seq;

        // Phase 1: Normal traffic
        transition_to(AXIS_SEQ_STATE_NORMAL);
        pre_seq = axis_burst_seq::type_id::create("pre_rst");
        if (!pre_seq.randomize() with {
            num_packets == local::pre_reset_packets;
            min_pkt_len == 4;
            max_pkt_len == 8;
        }) `uvm_error(get_type_name(), "Randomization failed")
        pre_seq.start(master_sqr);

        // Phase 2: Assert reset
        `uvm_info(get_type_name(), "Asserting reset", UVM_LOW)
        if (master_cfg.reset_polarity == AXIS_RESET_ACTIVE_LOW)
            vif.aresetn <= 1'b0;
        else
            vif.aresetn <= 1'b1;

        repeat (reset_duration_cycles) @(posedge vif.aclk);

        // Phase 3: Deassert reset
        `uvm_info(get_type_name(), "Deasserting reset", UVM_LOW)
        if (master_cfg.reset_polarity == AXIS_RESET_ACTIVE_LOW)
            vif.aresetn <= 1'b1;
        else
            vif.aresetn <= 1'b0;

        // Wait a few cycles for recovery
        repeat (5) @(posedge vif.aclk);

        // Phase 4: Recovery traffic
        transition_to(AXIS_SEQ_STATE_RECOVERY);
        post_seq = axis_burst_seq::type_id::create("post_rst");
        if (!post_seq.randomize() with {
            num_packets == local::post_reset_packets;
            min_pkt_len == 4;
            max_pkt_len == 8;
        }) `uvm_error(get_type_name(), "Randomization failed")
        post_seq.start(master_sqr);

        transition_to(AXIS_SEQ_STATE_DONE);
        `uvm_info(get_type_name(), "Reset recovery complete", UVM_LOW)
    endtask

endclass
```

- [ ] **Step 5: Write axis_full_stress_vseq.sv**

```systemverilog
// axis_vip/sequences/axis_full_stress_vseq.sv

class axis_full_stress_vseq extends axis_base_vseq;

    `uvm_object_utils(axis_full_stress_vseq)

    function new(string name = "axis_full_stress_vseq");
        super.new(name);
    endfunction

    task body();
        // Run multiple stress scenarios sequentially

        // Phase 1: Interleaved traffic
        begin
            axis_interleave_seq ilv;
            `uvm_info(get_type_name(), "Stress phase 1: interleaved traffic", UVM_LOW)
            ilv = axis_interleave_seq::type_id::create("stress_ilv");
            if (!ilv.randomize() with {
                num_streams == 4;
                total_packets == 16;
            }) `uvm_error(get_type_name(), "Randomization failed")
            ilv.start(master_sqr);
        end

        // Phase 2: Backpressure stress (master sends fast, slave backpressures)
        begin
            axis_backpressure_stress_seq bp;
            `uvm_info(get_type_name(), "Stress phase 2: backpressure", UVM_LOW)
            bp = axis_backpressure_stress_seq::type_id::create("stress_bp");
            if (!bp.randomize() with {
                num_packets == 20;
                pkt_len == 32;
            }) `uvm_error(get_type_name(), "Randomization failed")
            bp.start(master_sqr);
        end

        // Phase 3: Boundary conditions
        begin
            axis_boundary_seq bnd;
            `uvm_info(get_type_name(), "Stress phase 3: boundary conditions", UVM_LOW)
            bnd = axis_boundary_seq::type_id::create("stress_bnd");
            bnd.start(master_sqr);
        end

        // Phase 4: Burst
        begin
            axis_burst_seq burst;
            `uvm_info(get_type_name(), "Stress phase 4: burst", UVM_LOW)
            burst = axis_burst_seq::type_id::create("stress_burst");
            if (!burst.randomize() with {
                num_packets == 16;
                min_pkt_len == 1;
                max_pkt_len == 64;
            }) `uvm_error(get_type_name(), "Randomization failed")
            burst.start(master_sqr);
        end

        `uvm_info(get_type_name(), "Full stress test complete", UVM_LOW)
    endtask

endclass
```

- [ ] **Step 6: Commit**

```bash
git add axis_vip/sequences/axis_base_vseq.sv axis_vip/sequences/axis_master_slave_sync_vseq.sv axis_vip/sequences/axis_bandwidth_sweep_vseq.sv axis_vip/sequences/axis_reset_recovery_vseq.sv axis_vip/sequences/axis_full_stress_vseq.sv
git commit -m "feat(axis-vip): add virtual sequence layer with state machine support"
```

---

### Task 22: Dummy DUT and Testbench Top

**Files:**
- Create: `axis_vip/tb/axis_dummy_dut.sv`
- Create: `axis_vip/tb/tb_top.sv`

- [ ] **Step 1: Write axis_dummy_dut.sv (loopback)**

```systemverilog
// axis_vip/tb/axis_dummy_dut.sv
// Simple loopback: passes master output to slave input

module axis_dummy_dut #(
    parameter int TDATA_WIDTH = 32,
    parameter int TID_WIDTH   = 4,
    parameter int TDEST_WIDTH = 4,
    parameter int TUSER_WIDTH = 1
)(
    input  logic                       aclk,
    input  logic                       aresetn,
    // Master (input) side
    input  logic                       s_tvalid,
    output logic                       s_tready,
    input  logic [TDATA_WIDTH-1:0]     s_tdata,
    input  logic [TDATA_WIDTH/8-1:0]   s_tstrb,
    input  logic [TDATA_WIDTH/8-1:0]   s_tkeep,
    input  logic                       s_tlast,
    input  logic [TID_WIDTH-1:0]       s_tid,
    input  logic [TDEST_WIDTH-1:0]     s_tdest,
    input  logic [TUSER_WIDTH-1:0]     s_tuser,
    // Slave (output) side
    output logic                       m_tvalid,
    input  logic                       m_tready,
    output logic [TDATA_WIDTH-1:0]     m_tdata,
    output logic [TDATA_WIDTH/8-1:0]   m_tstrb,
    output logic [TDATA_WIDTH/8-1:0]   m_tkeep,
    output logic                       m_tlast,
    output logic [TID_WIDTH-1:0]       m_tid,
    output logic [TDEST_WIDTH-1:0]     m_tdest,
    output logic [TUSER_WIDTH-1:0]     m_tuser
);

    // Simple passthrough (loopback) with 1-cycle register
    always_ff @(posedge aclk or negedge aresetn) begin
        if (!aresetn) begin
            m_tvalid <= 1'b0;
            m_tdata  <= '0;
            m_tstrb  <= '0;
            m_tkeep  <= '0;
            m_tlast  <= 1'b0;
            m_tid    <= '0;
            m_tdest  <= '0;
            m_tuser  <= '0;
        end else if (s_tvalid && s_tready) begin
            m_tvalid <= 1'b1;
            m_tdata  <= s_tdata;
            m_tstrb  <= s_tstrb;
            m_tkeep  <= s_tkeep;
            m_tlast  <= s_tlast;
            m_tid    <= s_tid;
            m_tdest  <= s_tdest;
            m_tuser  <= s_tuser;
        end else if (m_tvalid && m_tready) begin
            m_tvalid <= 1'b0;
        end
    end

    // Backpressure: accept when output is idle or being consumed
    assign s_tready = !m_tvalid || m_tready;

endmodule
```

- [ ] **Step 2: Write tb_top.sv**

```systemverilog
// axis_vip/tb/tb_top.sv

`timescale 1ns/1ps

module tb_top;

    import uvm_pkg::*;
    `include "uvm_macros.svh"
    import axis_pkg::*;

    // Parameters
    localparam int TDATA_WIDTH = 32;
    localparam int TID_WIDTH   = 4;
    localparam int TDEST_WIDTH = 4;
    localparam int TUSER_WIDTH = 1;

    // Clock and reset
    logic aclk;
    logic aresetn;

    // Clock generation: 100MHz
    initial aclk = 0;
    always #5 aclk = ~aclk;

    // Reset generation
    initial begin
        aresetn = 1'b0;
        repeat (10) @(posedge aclk);
        aresetn = 1'b1;
    end

    // Interfaces
    axis_if #(
        .TDATA_WIDTH(TDATA_WIDTH),
        .TID_WIDTH(TID_WIDTH),
        .TDEST_WIDTH(TDEST_WIDTH),
        .TUSER_WIDTH(TUSER_WIDTH)
    ) master_if (
        .aclk(aclk),
        .aresetn(aresetn)
    );

    axis_if #(
        .TDATA_WIDTH(TDATA_WIDTH),
        .TID_WIDTH(TID_WIDTH),
        .TDEST_WIDTH(TDEST_WIDTH),
        .TUSER_WIDTH(TUSER_WIDTH)
    ) slave_if (
        .aclk(aclk),
        .aresetn(aresetn)
    );

    // DUT: loopback
    axis_dummy_dut #(
        .TDATA_WIDTH(TDATA_WIDTH),
        .TID_WIDTH(TID_WIDTH),
        .TDEST_WIDTH(TDEST_WIDTH),
        .TUSER_WIDTH(TUSER_WIDTH)
    ) dut (
        .aclk(aclk),
        .aresetn(aresetn),
        // Master VIP -> DUT input
        .s_tvalid(master_if.tvalid),
        .s_tready(master_if.tready),
        .s_tdata(master_if.tdata),
        .s_tstrb(master_if.tstrb),
        .s_tkeep(master_if.tkeep),
        .s_tlast(master_if.tlast),
        .s_tid(master_if.tid),
        .s_tdest(master_if.tdest),
        .s_tuser(master_if.tuser),
        // DUT output -> Slave VIP
        .m_tvalid(slave_if.tvalid),
        .m_tready(slave_if.tready),
        .m_tdata(slave_if.tdata),
        .m_tstrb(slave_if.tstrb),
        .m_tkeep(slave_if.tkeep),
        .m_tlast(slave_if.tlast),
        .m_tid(slave_if.tid),
        .m_tdest(slave_if.tdest),
        .m_tuser(slave_if.tuser)
    );

    // Pass virtual interfaces to UVM
    initial begin
        uvm_config_db#(virtual axis_if)::set(null, "uvm_test_top.env.master_agent*", "vif", master_if);
        uvm_config_db#(virtual axis_if)::set(null, "uvm_test_top.env.slave_agent*",  "vif", slave_if);
        uvm_config_db#(virtual axis_if)::set(null, "uvm_test_top.env.rst_handler",   "vif", master_if);
        uvm_config_db#(virtual axis_if)::set(null, "uvm_test_top.env.proto_checker", "vif", master_if);
        uvm_config_db#(virtual axis_if)::set(null, "uvm_test_top.env.bw_checker",    "vif", master_if);
    end

    // Run test
    initial begin
        run_test();
    end

    // Timeout
    initial begin
        #1000000;
        `uvm_fatal("TIMEOUT", "Simulation timed out")
    end

endmodule
```

- [ ] **Step 3: Commit**

```bash
git add axis_vip/tb/axis_dummy_dut.sv axis_vip/tb/tb_top.sv
git commit -m "feat(axis-vip): add loopback DUT and testbench top"
```

---

### Task 23: Base Test and Sanity Test

**Files:**
- Create: `axis_vip/tests/axis_base_test.sv`
- Create: `axis_vip/tests/axis_sanity_test.sv`

- [ ] **Step 1: Write axis_base_test.sv**

```systemverilog
// axis_vip/tests/axis_base_test.sv

class axis_base_test extends uvm_test;

    `uvm_component_utils(axis_base_test)

    axis_env    env;
    axis_config master_cfg;
    axis_config slave_cfg;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);

        // Create configs
        master_cfg = axis_config::type_id::create("master_cfg");
        slave_cfg  = axis_config::type_id::create("slave_cfg");

        // Configure master
        master_cfg.agent_mode = AXIS_MASTER;
        master_cfg.is_active  = UVM_ACTIVE;

        // Configure slave
        slave_cfg.agent_mode  = AXIS_SLAVE;
        slave_cfg.is_active   = UVM_ACTIVE;

        // Set configs in config_db
        uvm_config_db#(axis_config)::set(this, "env", "master_cfg", master_cfg);
        uvm_config_db#(axis_config)::set(this, "env", "slave_cfg",  slave_cfg);

        // Build env
        env = axis_env::type_id::create("env", this);
    endfunction

    function void end_of_elaboration_phase(uvm_phase phase);
        uvm_top.print_topology();
    endfunction

endclass
```

- [ ] **Step 2: Write axis_sanity_test.sv**

```systemverilog
// axis_vip/tests/axis_sanity_test.sv

class axis_sanity_test extends axis_base_test;

    `uvm_component_utils(axis_sanity_test)

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        // Default configs: zero idle master, always ready slave
    endfunction

    task run_phase(uvm_phase phase);
        axis_master_slave_sync_vseq vseq;

        phase.raise_objection(this);

        vseq = axis_master_slave_sync_vseq::type_id::create("vseq");
        vseq.master_sqr = env.master_agent.sqr;
        vseq.slave_sqr  = env.slave_agent.sqr;

        if (!vseq.randomize() with {
            num_packets == 4;
            pkt_len == 8;
        }) `uvm_error(get_type_name(), "Randomization failed")

        vseq.start(null);

        // Wait for all packets to propagate through DUT
        #200;

        phase.drop_objection(this);
    endtask

endclass
```

- [ ] **Step 3: Commit**

```bash
git add axis_vip/tests/axis_base_test.sv axis_vip/tests/axis_sanity_test.sv
git commit -m "feat(axis-vip): add base test and sanity test"
```

---

### Task 24: Remaining Test Cases

**Files:**
- Create: `axis_vip/tests/axis_backpressure_test.sv`
- Create: `axis_vip/tests/axis_bandwidth_test.sv`
- Create: `axis_vip/tests/axis_reset_test.sv`
- Create: `axis_vip/tests/axis_phase_jump_test.sv`
- Create: `axis_vip/tests/axis_full_regression_test.sv`

- [ ] **Step 1: Write axis_backpressure_test.sv**

```systemverilog
// axis_vip/tests/axis_backpressure_test.sv

class axis_backpressure_test extends axis_base_test;

    `uvm_component_utils(axis_backpressure_test)

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        // Configure slave with heavy backpressure
        slave_cfg.ready_gen_mode = READY_AFTER_VALID;
        slave_cfg.ready_delay_min = 2;
        slave_cfg.ready_delay_max = 10;
    endfunction

    task run_phase(uvm_phase phase);
        axis_backpressure_stress_seq stress_seq;

        phase.raise_objection(this);

        stress_seq = axis_backpressure_stress_seq::type_id::create("stress_seq");
        if (!stress_seq.randomize() with {
            num_packets == 10;
            pkt_len == 16;
        }) `uvm_error(get_type_name(), "Randomization failed")

        stress_seq.start(env.master_agent.sqr);

        #500;
        phase.drop_objection(this);
    endtask

endclass
```

- [ ] **Step 2: Write axis_bandwidth_test.sv**

```systemverilog
// axis_vip/tests/axis_bandwidth_test.sv

class axis_bandwidth_test extends axis_base_test;

    `uvm_component_utils(axis_bandwidth_test)

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        // Enable bandwidth checking
        master_cfg.bw_check_enable  = 1;
        master_cfg.bw_window_cycles = 500;
        master_cfg.bw_min_threshold = 0.1;
        master_cfg.bw_max_threshold = -1.0; // no upper limit
    endfunction

    task run_phase(uvm_phase phase);
        axis_bandwidth_sweep_vseq sweep_vseq;

        phase.raise_objection(this);

        sweep_vseq = axis_bandwidth_sweep_vseq::type_id::create("sweep_vseq");
        sweep_vseq.master_sqr = env.master_agent.sqr;
        sweep_vseq.slave_sqr  = env.slave_agent.sqr;
        sweep_vseq.master_cfg = master_cfg;
        sweep_vseq.slave_cfg  = slave_cfg;
        sweep_vseq.start(null);

        #200;
        phase.drop_objection(this);
    endtask

endclass
```

- [ ] **Step 3: Write axis_reset_test.sv**

```systemverilog
// axis_vip/tests/axis_reset_test.sv

class axis_reset_test extends axis_base_test;

    `uvm_component_utils(axis_reset_test)

    virtual axis_if vif;

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        master_cfg.hot_reset_enable = 1;
    endfunction

    function void connect_phase(uvm_phase phase);
        super.connect_phase(phase);
        if (!uvm_config_db#(virtual axis_if)::get(this, "", "vif", vif))
            // Try alternative path
            uvm_config_db#(virtual axis_if)::get(null, "uvm_test_top.env.master_agent*", "vif", vif);
    endfunction

    task run_phase(uvm_phase phase);
        axis_reset_recovery_vseq rst_vseq;

        phase.raise_objection(this);

        rst_vseq = axis_reset_recovery_vseq::type_id::create("rst_vseq");
        rst_vseq.master_sqr = env.master_agent.sqr;
        rst_vseq.slave_sqr  = env.slave_agent.sqr;
        rst_vseq.master_cfg = master_cfg;
        rst_vseq.slave_cfg  = slave_cfg;
        rst_vseq.vif = vif;

        if (!rst_vseq.randomize() with {
            pre_reset_packets == 4;
            post_reset_packets == 4;
            reset_duration_cycles == 10;
        }) `uvm_error(get_type_name(), "Randomization failed")

        rst_vseq.start(null);

        #200;
        phase.drop_objection(this);
    endtask

endclass
```

- [ ] **Step 4: Write axis_phase_jump_test.sv**

```systemverilog
// axis_vip/tests/axis_phase_jump_test.sv

class axis_phase_jump_test extends axis_base_test;

    `uvm_component_utils(axis_phase_jump_test)

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    task run_phase(uvm_phase phase);
        axis_burst_seq burst;

        phase.raise_objection(this);

        // Send some traffic
        burst = axis_burst_seq::type_id::create("burst");
        if (!burst.randomize() with {
            num_packets == 4;
            min_pkt_len == 4;
            max_pkt_len == 8;
        }) `uvm_error(get_type_name(), "Randomization failed")

        burst.start(env.master_agent.sqr);

        // Request phase jump (drain + jump)
        `uvm_info(get_type_name(), "Requesting phase jump to reset_phase", UVM_LOW)
        // In practice, phase jump target depends on the UVM phase schedule.
        // Here we demonstrate the drain mechanism by dropping objection
        // which effectively ends the run_phase.

        #100;
        phase.drop_objection(this);
    endtask

endclass
```

- [ ] **Step 5: Write axis_full_regression_test.sv**

```systemverilog
// axis_vip/tests/axis_full_regression_test.sv

class axis_full_regression_test extends axis_base_test;

    `uvm_component_utils(axis_full_regression_test)

    function new(string name, uvm_component parent);
        super.new(name, parent);
    endfunction

    function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        // Enable all checking
        master_cfg.bw_check_enable  = 1;
        master_cfg.bw_window_cycles = 500;
        slave_cfg.ready_gen_mode    = READY_WEIGHTED;
        slave_cfg.ready_weight      = 70;
    endfunction

    task run_phase(uvm_phase phase);
        axis_full_stress_vseq stress_vseq;

        phase.raise_objection(this);

        stress_vseq = axis_full_stress_vseq::type_id::create("stress_vseq");
        stress_vseq.master_sqr = env.master_agent.sqr;
        stress_vseq.slave_sqr  = env.slave_agent.sqr;
        stress_vseq.master_cfg = master_cfg;
        stress_vseq.slave_cfg  = slave_cfg;
        stress_vseq.start(null);

        #500;
        phase.drop_objection(this);
    endtask

endclass
```

- [ ] **Step 6: Commit**

```bash
git add axis_vip/tests/axis_backpressure_test.sv axis_vip/tests/axis_bandwidth_test.sv axis_vip/tests/axis_reset_test.sv axis_vip/tests/axis_phase_jump_test.sv axis_vip/tests/axis_full_regression_test.sv
git commit -m "feat(axis-vip): add backpressure, bandwidth, reset, phase jump, and regression tests"
```

---

### Task 25: Build System

**Files:**
- Create: `axis_vip/sim/filelist.f`
- Create: `axis_vip/sim/Makefile`

- [ ] **Step 1: Write filelist.f**

```
// axis_vip/sim/filelist.f

// Interface (compiled separately, not in package)
../src/axis_if.sv

// Package (includes all class files)
../src/axis_pkg.sv

// Tests (included after package)
../tests/axis_base_test.sv
../tests/axis_sanity_test.sv
../tests/axis_backpressure_test.sv
../tests/axis_bandwidth_test.sv
../tests/axis_reset_test.sv
../tests/axis_phase_jump_test.sv
../tests/axis_full_regression_test.sv

// DUT
../tb/axis_dummy_dut.sv

// Testbench top
../tb/tb_top.sv
```

- [ ] **Step 2: Write Makefile**

```makefile
# axis_vip/sim/Makefile
# Cross-tool simulation build

# Default test
TEST ?= axis_sanity_test
SEED ?= random
VERBOSITY ?= UVM_MEDIUM

# Tool selection: vcs, xcelium, questa
TOOL ?= vcs

# Common UVM args
UVM_ARGS = +UVM_TESTNAME=$(TEST) +UVM_VERBOSITY=$(VERBOSITY)

# File list
FILELIST = filelist.f

# ---- VCS ----
VCS_OPTS = -full64 -sverilog -ntb_opts uvm-1.2 -timescale=1ns/1ps \
           +incdir+../src +incdir+../sequences +incdir+../tests \
           -f $(FILELIST) -l compile.log

vcs_compile:
	vcs $(VCS_OPTS) -o simv

vcs_run: vcs_compile
	./simv $(UVM_ARGS) +ntb_random_seed=$(SEED) -l run.log

# ---- Xcelium ----
XRUN_OPTS = -sv -uvm -64bit -timescale 1ns/1ps \
            +incdir+../src +incdir+../sequences +incdir+../tests \
            -f $(FILELIST) -l compile.log

xcelium_compile:
	xrun -compile $(XRUN_OPTS)

xcelium_run:
	xrun $(XRUN_OPTS) $(UVM_ARGS) -svseed $(SEED) -l run.log

# ---- Questa ----
QUESTA_OPTS = -sv -timescale 1ns/1ps \
              +incdir+../src +incdir+../sequences +incdir+../tests

questa_compile:
	vlib work
	vlog $(QUESTA_OPTS) -f $(FILELIST) -l compile.log

questa_run: questa_compile
	vsim -c -do "run -all; quit" $(UVM_ARGS) -sv_seed $(SEED) work.tb_top -l run.log

# ---- Convenience targets ----
ifeq ($(TOOL),vcs)
compile: vcs_compile
run: vcs_run
else ifeq ($(TOOL),xcelium)
compile: xcelium_compile
run: xcelium_run
else ifeq ($(TOOL),questa)
compile: questa_compile
run: questa_run
endif

# Run all tests
regression:
	$(MAKE) run TEST=axis_sanity_test
	$(MAKE) run TEST=axis_backpressure_test
	$(MAKE) run TEST=axis_bandwidth_test
	$(MAKE) run TEST=axis_reset_test
	$(MAKE) run TEST=axis_phase_jump_test
	$(MAKE) run TEST=axis_full_regression_test

clean:
	rm -rf simv simv.dbin csrc work xcelium.d INCA_libs *.log *.key DVEfiles ucli.key

.PHONY: compile run regression clean vcs_compile vcs_run xcelium_compile xcelium_run questa_compile questa_run
```

- [ ] **Step 3: Commit**

```bash
git add axis_vip/sim/filelist.f axis_vip/sim/Makefile
git commit -m "feat(axis-vip): add cross-tool build system (VCS/Xcelium/Questa)"
```

---

### Task 26: Final Integration Verification

- [ ] **Step 1: Verify all files exist**

```bash
find axis_vip -name "*.sv" -o -name "*.f" -o -name "Makefile" | sort
```

Expected output: all 39 files listed in the file structure.

- [ ] **Step 2: Verify package include order has no missing files**

```bash
grep '`include' axis_vip/src/axis_pkg.sv | wc -l
```

Expected: 28 includes (20 src + 14 sequences - 6 test files not in package = correctly counted in the package)

- [ ] **Step 3: Check for type consistency across files**

```bash
grep -rn "axis_config" axis_vip/src/ | grep "cfg" | head -20
```

Verify: all components use `axis_config cfg` and get it from `uvm_config_db`.

- [ ] **Step 4: Final commit with all files**

```bash
git add -A axis_vip/
git commit -m "feat(axis-vip): complete AXI4-Stream UVM VIP implementation"
```
